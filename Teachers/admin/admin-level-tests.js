(function(){
'use strict';
var tests=[],loaded=false,loading=false,activeFilter='all',activeDetail=null;
var endpoint='https://api.willenaenglish.com/.netlify/functions/admin_classes';
var byId=function(id){return document.getElementById(id)};
function esc(value){return String(value==null?'':value).replace(/[&<>"']/g,function(char){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]})}
function api(url,options){var fn=window.WillenaAPI&&window.WillenaAPI.fetch?window.WillenaAPI.fetch:window.fetch.bind(window);return fn(url,Object.assign({credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json'}},options||{})).then(function(response){return response.json().catch(function(){return{}}).then(function(data){if(!response.ok||data.success===false)throw new Error(data.error||'Request failed '+response.status);return data})})}
function labelSource(source){return source==='prospective'?'Online test':source==='internal'?'Willena Student':'Visitor'}
function labelStatus(status){return status==='completed'?'Completed':'In progress'}
function publicLevel(level){var n=Number(level);if(!n)return'—';return n<=2?'S'+n:String(n-2)}
function fullLevel(level){var n=Number(level);if(!n)return'Not estimated';return n<=2?'Starter '+n:'Level '+(n-2)}
function when(row){var raw=row.completed_at||row.started_at||row.updated_at;if(!raw)return'—';try{return new Intl.DateTimeFormat(undefined,{year:'numeric',month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(raw))}catch(_){return raw}}
function name(row){return row.student_name||row.korean_name||row.username||'Student'}
function skillFor(value){return({vocabulary:'vocabulary',grammar:'grammar',grammar_error:'grammar',question_response:'grammar',listening:'listening',reading:'reading',sentence_unscramble:'sentence_building',speaking:'speaking',writing:'writing'})[String(value||'')]||'other'}
function skillName(value){return({vocabulary:'Vocabulary',grammar:'Grammar',listening:'Listening',reading:'Reading',sentence_building:'Sentence making',speaking:'Speaking',writing:'Writing',other:'Other'})[value]||String(value||'Other')}
function filtered(){var query=(byId('levelTestSearch').value||'').trim().toLowerCase();return tests.filter(function(row){var hay=[name(row),row.korean_name,row.username,row.class_name,row.school,row.phone].join(' ').toLowerCase(),matches=activeFilter==='all'||activeFilter==='new'&&row.is_new||row.source===activeFilter;return matches&&(!query||hay.indexOf(query)>=0)})}
function render(){var rows=filtered(),body=byId('levelTestRows');byId('levelTestNewCount').textContent=tests.filter(function(row){return row.is_new}).length;if(!rows.length){body.innerHTML='<tr><td colspan="5" class="level-test-empty">No matching level tests.</td></tr>';return}body.innerHTML=rows.map(function(row){var student=name(row);return'<tr class="'+(row.is_new?'is-new':'')+'" data-level-test-id="'+esc(row.id)+'"><td><div class="level-test-person"><div class="ava">'+esc(student.charAt(0).toUpperCase())+'</div><div><b>'+esc(student)+'</b>'+(row.is_new?'<span class="level-test-new">New</span>':'')+'<div class="mut">'+esc(row.grade||row.school||'')+'</div></div></div></td><td>'+esc(when(row))+'</td><td><span class="level-test-level">'+publicLevel(row.recommended_level)+'</span></td><td><span class="level-test-status '+esc(row.status)+'">'+labelStatus(row.status)+'</span></td><td><span class="level-test-source '+esc(row.source)+'">'+labelSource(row.source)+'</span></td></tr>'}).join('');body.querySelectorAll('[data-level-test-id]').forEach(function(element){element.onclick=function(){open(element.getAttribute('data-level-test-id'))}})}
function answer(value){if(Array.isArray(value))return value.join(' ');if(value&&typeof value==='object')return JSON.stringify(value);if(value===null||value===undefined||value==='')return'No answer';return String(value)}
function sentenceText(value){if(typeof value==='string'){var encoded=value.trim();if(encoded.charAt(0)==='['&&encoded.charAt(encoded.length-1)===']'){try{var decoded=JSON.parse(encoded);if(Array.isArray(decoded))value=decoded}catch(_){}}}return answer(value).normalize('NFKC').toLocaleLowerCase('en-US').replace(/[.,!?;:。！？、，；：]+$/gu,'').replace(/\s+/g,' ').trim()}
function repairResponse(row){if(String(row.item_type||row.question_type)!=='sentence_unscramble')return row;row.is_correct=sentenceText(row.selected_answer)===sentenceText(row.correct_answer);return row}
function probabilities(rows,maxLevel){if(!rows.length)return[];var ceiling=Math.max(1,Math.min(12,Number(maxLevel)||12)),scores=[];for(var level=1;level<=ceiling;level++){var log=0;rows.forEach(function(row){var p=1/(1+Math.exp((Number(row.question_level||row.level)-level)*1.12));log+=Math.log(Math.max(.025,Math.min(.975,row.is_correct?p:1-p)))});scores.push({level:level,log:log})}var max=Math.max.apply(null,scores.map(function(x){return x.log})),weighted=scores.map(function(x){return{level:x.level,w:Math.exp(x.log-max)}}),total=weighted.reduce(function(sum,x){return sum+x.w},0)||1;return weighted.map(function(x){return{level:x.level,pct:x.w/total*100}}).sort(function(a,b){return b.pct-a.pct})}
function estimate(rows,fallback){if(!rows.length)return Number(fallback)||0;var highest=Math.max.apply(null,rows.map(function(x){return Number(x.question_level)||1}));return(probabilities(rows,highest)[0]||{}).level||Number(fallback)||1}
function infoField(label,value){return'<div><small>'+esc(label)+'</small><b>'+esc(value||'Not recorded')+'</b></div>'}
function evidenceRow(row,good){if(!row)return'';return'<div class="level-test-evidence '+(good?'good':'needs')+'"><i>'+(good?'✓':'!')+'</i><div><b>'+esc(row.prompt_snapshot||'Question '+row.answer_index)+'</b>'+(good?'':'<small>Student: '+esc(answer(row.selected_answer))+'<br>Correct: '+esc(answer(row.correct_answer))+'</small>')+'</div></div>'}
function renderMini(data){var responses=Array.isArray(data.responses)?data.responses.map(repairResponse):[],candidate=data.candidate||{},attempt=data.attempt||{},overall=estimate(responses,attempt.recommended_level||attempt.display_level),groups={};data.responses=responses;responses.forEach(function(row){var key=row.skill||skillFor(row.item_type||row.question_type);(groups[key]||(groups[key]=[])).push(row)});var order=['vocabulary','grammar','listening','reading','sentence_building','writing','speaking','other'];var skills=order.filter(function(key){return groups[key]&&groups[key].length}).map(function(key){var rows=groups[key],right=rows.filter(function(x){return x.is_correct===true}),wrong=rows.filter(function(x){return x.is_correct!==true}),level=rows.length>=3?fullLevel(estimate(rows,overall)):'Limited evidence';return'<article class="level-test-mini-skill"><div class="level-test-mini-skill-head"><h4>'+skillName(key)+'</h4><span>'+level+'</span></div>'+evidenceRow(right.slice().sort(function(a,b){return Number(b.question_level)-Number(a.question_level)})[0],true)+evidenceRow(wrong.slice().sort(function(a,b){return Number(a.question_level)-Number(b.question_level)})[0],false)+'</article>'}).join('');byId('levelTestDetailBody').innerHTML='<section class="level-test-profile"><h3>Student information</h3><div class="level-test-info">'+infoField('English name',candidate.student_name)+infoField('Korean name',candidate.korean_name)+infoField('Phone',candidate.phone)+infoField('School',candidate.school_name)+infoField('Grade',candidate.school_grade)+infoField('Class',candidate.class_name)+infoField('Username',candidate.username)+infoField('Email',candidate.email)+'</div></section><section class="level-test-mini-hero"><small>ESTIMATED STARTING LEVEL</small><strong>'+esc(fullLevel(overall))+'</strong><p>Based on the difficulty of the questions answered correctly and incorrectly.</p></section><section class="level-test-profile"><h3>Diagnostic evidence</h3><p class="level-test-explain">A strong answer shows the highest successful evidence in that skill. “Needs work” shows the earliest missed evidence—not just a total score.</p><div class="level-test-mini-skills">'+(skills||'<p class="mut">No answer-level evidence has been recorded yet.</p>')+'</div></section>'}
function renderFullResults(){if(!activeDetail)return;var rows=activeDetail.responses||[],groups={};rows.forEach(function(row){var key=row.skill||skillFor(row.item_type||row.question_type);(groups[key]||(groups[key]=[])).push(row)});var order=['vocabulary','grammar','listening','reading','sentence_building','writing','speaking','other'];byId('levelTestResultsMeta').textContent=name(activeDetail.candidate||{})+' · '+rows.length+' answers';byId('levelTestResultsBody').innerHTML=order.filter(function(key){return groups[key]&&groups[key].length}).map(function(key){var category=groups[key],correct=category.filter(function(x){return x.is_correct===true}).length;return'<section class="level-test-result-category"><div class="level-test-result-category-head"><h3>'+skillName(key)+'</h3><span>'+correct+' of '+category.length+' correct</span></div>'+category.map(function(row){return'<article class="level-test-answer '+(row.is_correct?'correct':'wrong')+'"><div class="level-test-answer-number">'+esc(row.answer_index)+'</div><div><h4>'+esc(row.prompt_snapshot||'Question')+'</h4><dl><div><dt>Student answer</dt><dd>'+esc(answer(row.selected_answer))+'</dd></div><div><dt>Correct answer</dt><dd>'+esc(answer(row.correct_answer))+'</dd></div></dl></div><span class="level-test-answer-state">'+(row.is_correct?'Correct':'Needs work')+'</span></article>'}).join('')+'</section>'}).join('')||'<p class="level-test-empty">No responses were recorded.</p>';byId('levelTestResults').classList.add('show');byId('levelTestResults').setAttribute('aria-hidden','false')}
function open(id){var row=tests.find(function(item){return String(item.id)===String(id)});if(!row)return;activeDetail=null;byId('levelTestDetailSource').className='level-test-source '+row.source;byId('levelTestDetailSource').textContent=labelSource(row.source);byId('levelTestDetailName').textContent=name(row);byId('levelTestDetailMeta').textContent=[row.class_name||row.school,when(row)].filter(Boolean).join(' · ');byId('levelTestDetailBody').innerHTML='<p class="level-test-empty">Loading diagnostic report…</p>';byId('levelTestFullReport').disabled=true;byId('levelTestFullResults').disabled=true;byId('levelTestResults').classList.remove('show');var drawer=byId('levelTestDrawer');drawer.classList.add('show');drawer.setAttribute('aria-hidden','false');api(endpoint,{method:'POST',body:JSON.stringify({action:'level_test_detail',source:row.source,attempt_id:row.id})}).then(function(data){activeDetail=data;row.is_new=false;render();renderMini(data);byId('levelTestFullReport').disabled=false;byId('levelTestFullResults').disabled=false}).catch(function(error){byId('levelTestDetailBody').innerHTML='<p class="level-test-empty">'+esc(error.message)+'</p>'})}
function closeResults(){byId('levelTestResults').classList.remove('show');byId('levelTestResults').setAttribute('aria-hidden','true')}
function close(){closeResults();var drawer=byId('levelTestDrawer');drawer.classList.remove('show');drawer.setAttribute('aria-hidden','true');activeDetail=null}
function load(force){if(loading||loaded&&!force)return;loading=true;byId('levelTestRows').innerHTML='<tr><td colspan="5" class="level-test-empty">Loading real test records…</td></tr>';api(endpoint+'?action=list_level_tests').then(function(data){tests=Array.isArray(data.tests)?data.tests:[];loaded=true;render()}).catch(function(error){byId('levelTestRows').innerHTML='<tr><td colspan="5" class="level-test-empty">'+esc(error.message)+'</td></tr>'}).finally(function(){loading=false})}
document.querySelectorAll('[data-page="levelTests"]').forEach(function(button){button.addEventListener('click',function(){load(false)})});
byId('levelTestSearch').addEventListener('input',render);byId('levelTestRefresh').addEventListener('click',function(){load(true)});
byId('levelTestFilters').querySelectorAll('[data-test-filter]').forEach(function(button){button.onclick=function(){activeFilter=button.dataset.testFilter;byId('levelTestFilters').querySelectorAll('button').forEach(function(x){x.classList.toggle('active',x===button)});render()}});
byId('levelTestFullReport').onclick=function(){if(!activeDetail)return;window.open('/free-level-test/admin-report/?teacher=1&admin=1&source='+encodeURIComponent(activeDetail.source)+'&attempt_id='+encodeURIComponent(activeDetail.attempt.id),'_blank','noopener')};
byId('levelTestFullResults').onclick=renderFullResults;byId('levelTestResultsBack').onclick=closeResults;
document.querySelectorAll('[data-close-level-test]').forEach(function(button){button.addEventListener('click',close)});document.addEventListener('keydown',function(event){if(event.key==='Escape'){if(byId('levelTestResults').classList.contains('show'))closeResults();else close()}});
})();
