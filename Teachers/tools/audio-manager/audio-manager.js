(function(){
'use strict';
var CONTENT_ROOT='https://gxwfsqxyuufqtitspfqg.supabase.co/rest/v1/';
var CONTENT_KEY=['sb_publishable_','G-FYhHfDL4OGdL892gY1Zg_','epdbEeqO'].join('');
var API_ORIGIN='https://api.willenaenglish.com';
var HISTORY_KEY='willena_audio_manager_runs_v1';
var state={books:[],series:{},book:null,units:[],tasks:[],scanning:false,generating:false,uploaded:0,failed:0};
var $=function(id){return document.getElementById(id);};
var auth=$('auth'),authMsg=$('authMsg'),bookSelect=$('bookSelect'),unitFilter=$('unitFilter'),tbody=$('tbody'),emptyState=$('emptyState');
var totalCount=$('totalCount'),existingCount=$('existingCount'),missingCount=$('missingCount'),uploadedCount=$('uploadedCount'),failedCount=$('failedCount');
var progressLabel=$('progressLabel'),progressNumbers=$('progressNumbers'),progressBar=$('progressBar');
function text(v){return String(v==null?'':v).trim();}
function esc(v){return text(v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function uniq(a){var o=[],s={};(a||[]).forEach(function(v){v=text(v);if(v&&!s[v]){s[v]=1;o.push(v);}});return o;}
function chunks(a,n){var o=[];for(var i=0;i<a.length;i+=n)o.push(a.slice(i,i+n));return o;}
function normalizeKey(v){return text(v).replace(/\.mp3$/i,'').toLowerCase().replace(/\s+/g,'_').replace(/[^a-z0-9_\-]/g,'');}
function looksSentence(s){s=text(s);return !!s&&(/[.?!]$/.test(s)||/^(what|where|when|who|why|how|do|does|did|is|are|was|were|can|could|will|would|should|have|has|had|i|you|he|she|it|we|they)\b/i.test(s));}
function playablePattern(s){s=text(s);return looksSentence(s)&&!(/[+{}]|→/.test(s));}
function sleep(ms){return new Promise(function(r){setTimeout(r,ms);});}
function toast(m,bad){var e=$('toast');e.textContent=m;e.style.background=bad?'#912018':'#101828';e.style.display='block';clearTimeout(e._t);e._t=setTimeout(function(){e.style.display='none';},3200);}
function apiFetch(path,opts){if(window.WillenaAPI&&typeof window.WillenaAPI.fetch==='function')return window.WillenaAPI.fetch(path,opts);return fetch(path,opts);}
async function retryFetch(path,opts,label){var last;for(var attempt=1;attempt<=4;attempt++){try{var r=await apiFetch(path,opts);if(r.ok||!(r.status===429||r.status>=500))return r;last=new Error(label+' '+r.status);try{last.responseBody=await r.clone().text();}catch(_){}}catch(e){last=e;}if(attempt<4)await sleep(700*Math.pow(2,attempt-1));}throw last||new Error(label+' failed');}
async function verify(){try{var wr=await apiFetch('/.netlify/functions/supabase_auth?action=whoami&_='+Date.now());var who=await wr.json().catch(function(){return{};});if(!who.success||!who.user_id){location.href='/Teachers/login.html?redirect='+encodeURIComponent(location.pathname+location.search);return false;}try{localStorage.setItem('userId',who.user_id);}catch(_){}var pr=await apiFetch('/.netlify/functions/supabase_auth?action=get_profile&user_id='+encodeURIComponent(who.user_id)+'&_='+Date.now());var p=await pr.json().catch(function(){return{};});var role=text(p.role).toLowerCase();if(!p.success||p.approved!==true||['teacher','admin'].indexOf(role)<0){location.href='/Teachers/profile.html';return false;}auth.hidden=true;$('connection').textContent='● Connected';return true;}catch(e){authMsg.textContent='Could not verify this account.';return false;}}
async function rest(table,params,paged){var all=[],offset=0,size=1000;do{var u=new URL(CONTENT_ROOT+table);Object.keys(params||{}).forEach(function(k){if(params[k]!=null&&params[k]!=='')u.searchParams.set(k,params[k]);});if(paged!==false){u.searchParams.set('limit',size);u.searchParams.set('offset',offset);}var r=await fetch(u.toString(),{headers:{apikey:CONTENT_KEY,Authorization:'Bearer '+CONTENT_KEY},cache:'no-store'});if(!r.ok)throw new Error('Content DB '+r.status+' ('+table+')');var rows=await r.json();if(!Array.isArray(rows))rows=[];all=all.concat(rows);if(paged===false||rows.length<size)break;offset+=size;}while(offset<100000);return all;}
async function fetchByIds(table,select,ids){ids=uniq(ids);var out=[];for(var g of chunks(ids,80)){var rows=await rest(table,{select:select,id:'in.('+g.join(',')+')'},false);out=out.concat(rows);}return out;}
async function fetchDialogueTurns(ids){ids=uniq(ids);var out=[];for(var g of chunks(ids,80)){var rows=await rest('source_dialogue_turns',{select:'id,dialogue_id,turn_number,speaker_label,source_text,sentence_id,metadata',dialogue_id:'in.('+g.join(',')+')',order:'dialogue_id.asc,turn_number.asc'},true);out=out.concat(rows);}return out;}
function byId(rows){var m={};(rows||[]).forEach(function(r){if(r&&r.id)m[String(r.id)]=r;});return m;}
function unitLabel(id){var u=state.units.find(function(x){return String(x.id)===String(id);});return u?'Unit '+u.unit_number+(u.title?' · '+u.title:''):'—';}
function setProgress(d,t,l){progressBar.style.width=(t?Math.round(d/t*100):0)+'%';progressNumbers.textContent=t?(d+' / '+t):'';if(l)progressLabel.textContent=l;}
async function loadBooks(){var b=await Promise.all([rest('content_series',{select:'id,name,source_key',order:'name.asc'},true),rest('content_books',{select:'id,series_id,title,book_number,source_key,status,internal_level_id,public_level',status:'in.(review,published)',order:'title.asc'},true)]);state.series=byId(b[0]);state.books=b[1];state.books.sort(function(a,bk){var sa=text((state.series[a.series_id]||{}).name),sb=text((state.series[bk.series_id]||{}).name);return sa.localeCompare(sb)||Number(a.book_number||0)-Number(bk.book_number||0)||text(a.title).localeCompare(text(bk.title));});bookSelect.innerHTML='<option value="">Choose a book…</option>'+state.books.map(function(x){var s=state.series[x.series_id]||{};return '<option value="'+esc(x.id)+'">'+esc((s.name?s.name+' · ':'')+x.title)+'</option>';}).join('');}
function addTask(map,raw){var key=text(raw.key||raw.spoken),norm=normalizeKey(key),spoken=text(raw.spoken);if(!norm||!spoken)return;var t=map[norm];if(!t)t=map[norm]={key:key,norm:norm,spoken:spoken,type:raw.type||'sentence',unitIds:[],tracks:[],activities:[],sources:[],state:'unchecked',url:'',selected:true,error:''};t.unitIds=uniq(t.unitIds.concat(raw.unitId?[raw.unitId]:[]));t.tracks=uniq(t.tracks.concat(raw.track!=null&&raw.track!==''?[String(raw.track)]:[]));t.activities=uniq(t.activities.concat(raw.activity?[raw.activity]:[]));t.sources=uniq(t.sources.concat(raw.source?[raw.source]:[]));if(t.type==='sentence'&&raw.type==='listening')t.type='listening';if(t.type==='sentence'&&raw.type==='dialogue')t.type='dialogue';}
async function loadManifest(bookId){state.book=state.books.find(function(b){return b.id===bookId;})||null;state.tasks=[];state.uploaded=0;state.failed=0;renderSummary();renderRows();$('scanBtn').disabled=true;$('generateBtn').disabled=true;if(!state.book)return;setProgress(0,0,'Reading '+state.book.title+' from the book database…');try{var base=await Promise.all([rest('content_units',{select:'id,book_id,unit_number,title,status,source_key',book_id:'eq.'+bookId,status:'in.(review,published)',order:'unit_number.asc'},true),rest('source_content_occurrences',{select:'id,unit_id,track_number,activity_label,occurrence_type,source_text,lexical_entry_id,sentence_id,passage_id,skill,status,metadata',book_id:'eq.'+bookId,status:'in.(review,published)',order:'unit_id.asc,page_number.asc.nullslast,track_number.asc.nullslast'},true),rest('assessment_items',{select:'id,unit_id,item_type,prompt_text,metadata,status',book_id:'eq.'+bookId,status:'in.(review,published)'},true),rest('source_dialogues',{select:'id,unit_id,title,track_number,activity_label,status,metadata',book_id:'eq.'+bookId,status:'in.(review,published)'},true)]);state.units=base[0];var occ=base[1],assess=base[2],dialogues=base[3];unitFilter.innerHTML='<option value="">All units</option>'+state.units.map(function(u){return '<option value="'+esc(u.id)+'">'+esc('Unit '+u.unit_number+(u.title?' · '+u.title:''))+'</option>';}).join('');var details=await Promise.all([fetchByIds('lexical_entries','id,canonical_text,source_key,status',occ.map(function(o){return o.lexical_entry_id;})),fetchByIds('sentences','id,text,canonical_text,audio_key,source_key,status',occ.map(function(o){return o.sentence_id;})),fetchByIds('passages','id,title,body,audio_key,source_key,status',occ.map(function(o){return o.passage_id;})),fetchDialogueTurns(dialogues.map(function(d){return d.id;}))]);var lex=byId(details[0]),sent=byId(details[1]),pass=byId(details[2]),dm=byId(dialogues),map={};occ.forEach(function(o){var common={unitId:o.unit_id,track:o.track_number,activity:o.activity_label,source:'book content'};if(o.occurrence_type==='lexical_entry'){var l=lex[String(o.lexical_entry_id)]||{},word=text(l.canonical_text||o.source_text);if(word)addTask(map,Object.assign({},common,{key:word,spoken:word,type:'vocabulary'}));}else if(o.occurrence_type==='sentence'){var s=sent[String(o.sentence_id)]||{},sentence=text(s.canonical_text||s.text||o.source_text),key=text(s.audio_key)||sentence;if(sentence)addTask(map,Object.assign({},common,{key:key,spoken:sentence,type:o.skill==='listening'?'listening':'sentence'}));}else if(o.occurrence_type==='passage'){var p=pass[String(o.passage_id)]||{},body=text(p.body||o.source_text),pk=text(p.audio_key);if(body&&pk)addTask(map,Object.assign({},common,{key:pk,spoken:body,type:'passage'}));}else if(o.occurrence_type==='pattern'&&playablePattern(o.source_text)){addTask(map,Object.assign({},common,{key:o.source_text,spoken:o.source_text,type:'sentence'}));}});details[3].forEach(function(turn){var d=dm[String(turn.dialogue_id)]||{},s=text(turn.source_text);if(s)addTask(map,{key:s,spoken:s,type:'dialogue',unitId:d.unit_id,track:d.track_number,activity:d.activity_label||d.title||'Dialogue',source:'dialogue turn'});});assess.forEach(function(r){if(text(r.item_type)!=='listening')return;var m=r.metadata||{},tr=text(m.transcript||m.audio_text||m.tts_text);if(tr)addTask(map,{key:text(m.audio_key||m.audioKey||m.tts_key||m.ttsKey)||tr,spoken:tr,type:'listening',unitId:r.unit_id,track:m.track_number,activity:text(r.prompt_text)||'Listening assessment',source:'assessment'});});state.tasks=Object.keys(map).map(function(k){return map[k];}).sort(function(a,b){return a.type.localeCompare(b.type)||a.norm.localeCompare(b.norm);});renderSummary();renderRows();$('scanBtn').disabled=!state.tasks.length;if(!state.tasks.length){progressLabel.textContent='No playable audio content found for this book.';return;}await scanR2();}catch(e){console.error(e);progressLabel.textContent='Could not build audio manifest.';toast(e.message,true);}}
async function lookupExisting(tasks,onProgress){var results={},done=0;for(var g of chunks(tasks,100)){var words=g.map(function(t){return t.key;});var r=await retryFetch('/.netlify/functions/get_audio_urls',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',cache:'no-store',body:JSON.stringify({words:words})},'R2 lookup');if(!r.ok)throw new Error('R2 lookup failed ('+r.status+')');var d=await r.json();Object.keys(d.results||{}).forEach(function(k){results[k]=d.results[k];});done+=g.length;if(onProgress)onProgress(done,tasks.length);}return results;}
async function directExists(taskOrKey){var norm=typeof taskOrKey==='string'?normalizeKey(taskOrKey):taskOrKey.norm;if(!norm)return{exists:false,url:''};var url=API_ORIGIN+'/audio/'+encodeURIComponent(norm+'.mp3')+'?_='+Date.now();try{var r=await fetch(url,{method:'HEAD',credentials:'omit',cache:'no-store'});return{exists:r.ok,url:r.ok?url.replace(/[?].*$/,''):''};}catch(_){return{exists:false,url:''};}}
async function scanR2(){if(state.scanning||!state.tasks.length)return;state.scanning=true;$('scanBtn').disabled=true;$('generateBtn').disabled=true;state.tasks.forEach(function(t){if(t.state!=='uploaded')t.state='checking';});renderRows();try{var results=await lookupExisting(state.tasks,function(d,t){setProgress(d,t,'Checking R2 for existing MP3 files…');});state.tasks.forEach(function(t){if(t.state==='uploaded')return;var info=results[t.key];if(info&&info.exists&&info.url){t.state='existing';t.url=info.url;t.selected=false;}else{t.state='missing';t.url='';t.selected=true;}t.error='';});setProgress(state.tasks.length,state.tasks.length,'R2 scan complete. Only missing files are selected.');}catch(e){toast(e.message,true);progressLabel.textContent='R2 scan failed.';}finally{state.scanning=false;$('scanBtn').disabled=false;syncGenerateButton();renderSummary();renderRows();}}
function syncGenerateButton(){$('generateBtn').disabled=state.generating||!state.tasks.some(function(t){return t.selected&&(t.state==='missing'||t.state==='failed');});}
function currentTasks(){var q=text($('rowSearch').value).toLowerCase(),type=$('typeFilter').value,st=$('stateFilter').value,unit=unitFilter.value;return state.tasks.filter(function(t){if(type&&t.type!==type)return false;if(st&&t.state!==st)return false;if(unit&&t.unitIds.indexOf(unit)<0)return false;if(q&&[t.spoken,t.key,t.norm,t.tracks.join(' '),t.activities.join(' ')].join(' ').toLowerCase().indexOf(q)<0)return false;return true;});}
function renderRows(){if(!state.tasks.length){tbody.innerHTML='';emptyState.style.display='block';return;}emptyState.style.display='none';tbody.innerHTML=currentTasks().map(function(t){var units=t.unitIds.map(unitLabel).join('<br>')||'—',tracks=t.tracks.length?'Track '+t.tracks.join(', '):'No track',label={unchecked:'Not checked',checking:'Checking…',existing:'Existing',missing:'Missing',running:'Creating…',uploaded:'Uploaded',failed:'Failed'}[t.state]||t.state,cl=t.state==='checking'?'running':t.state;return '<tr><td><input class="row-check" type="checkbox" data-key="'+esc(t.norm)+'" '+(t.selected?'checked':'')+' '+(['missing','failed'].indexOf(t.state)>=0?'':'disabled')+'></td><td class="main">'+esc(t.spoken)+'<div class="sub">'+esc(t.sources.join(' · '))+'</div></td><td><span class="chip">'+esc(t.type)+'</span></td><td>'+units+'<div class="sub">'+esc(tracks)+'</div></td><td>'+esc(t.activities.join(' · ')||'—')+'</td><td><code>'+esc(t.norm)+'.mp3</code></td><td><span class="chip '+esc(cl)+'">'+esc(label)+'</span>'+(t.error?'<div class="sub">'+esc(t.error)+'</div>':'')+'</td><td>'+(t.url?'<button class="play" data-play="'+esc(t.url)+'">▶ Play</button>':'—')+'</td></tr>';}).join('');tbody.querySelectorAll('.row-check').forEach(function(cb){cb.addEventListener('change',function(){var t=state.tasks.find(function(x){return x.norm===cb.dataset.key;});if(t)t.selected=cb.checked;syncGenerateButton();});});tbody.querySelectorAll('[data-play]').forEach(function(b){b.addEventListener('click',function(){playUrl(b.dataset.play,b);});});}
function renderSummary(){totalCount.textContent=state.tasks.length;existingCount.textContent=state.tasks.filter(function(t){return t.state==='existing';}).length;missingCount.textContent=state.tasks.filter(function(t){return t.state==='missing';}).length;uploadedCount.textContent=state.uploaded;failedCount.textContent=state.failed;}
function playUrl(url,b){try{if(window.__audioManagerAudio)window.__audioManagerAudio.pause();var a=new Audio(url);window.__audioManagerAudio=a;b.textContent='■ Playing';a.onended=function(){b.textContent='▶ Play';};a.onerror=function(){b.textContent='▶ Play';toast('Could not play that audio file.',true);};a.play();}catch(e){toast(e.message,true);}}
async function tts(task){var voice=text($('voiceId').value);if(voice){try{localStorage.setItem('ttsVoiceId',voice);}catch(_){}}var payload={text:task.spoken,model_id:task.type==='vocabulary'?'eleven_v3':'eleven_turbo_v2_5'};if(voice)payload.voice_id=voice;var r=await retryFetch('/.netlify/functions/eleven_labs_proxy',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',cache:'no-store',body:JSON.stringify(payload)},'ElevenLabs');var d=await r.json().catch(function(){return{};});if(!r.ok||!d.audio)throw new Error(d.error||d.details||'ElevenLabs failed ('+r.status+')');return d.audio;}
async function upload(task,audio){var r=await retryFetch('/.netlify/functions/upload_audio',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',cache:'no-store',body:JSON.stringify({word:task.key,fileDataBase64:audio})},'R2 upload');var d=await r.json().catch(function(){return{};});if(!r.ok)throw new Error(d.error||'R2 upload failed ('+r.status+')');return d.url||'';}
async function processTask(task){task.state='running';task.error='';renderRows();try{var before=await directExists(task);if(before.exists){task.state='existing';task.url=before.url;task.selected=false;return{skipped:true};}var audio=await tts(task),after=await directExists(task);if(after.exists){task.state='existing';task.url=after.url;task.selected=false;return{skipped:true};}var url=await upload(task,audio);task.state='uploaded';task.url=url||API_ORIGIN+'/audio/'+encodeURIComponent(task.norm+'.mp3');task.selected=false;state.uploaded++;return{uploaded:true};}catch(e){task.state='failed';task.error=(e&&e.message)||String(e);task.selected=true;state.failed++;return{failed:true};}finally{renderSummary();renderRows();}}
async function generateMissing(){if(state.generating)return;var queue=state.tasks.filter(function(t){return t.selected&&(t.state==='missing'||t.state==='failed');});if(!queue.length)return;state.generating=true;state.uploaded=0;state.failed=0;$('generateBtn').disabled=true;$('scanBtn').disabled=true;var index=0,completed=0,skipped=0,workers=Math.max(1,Math.min(2,Number($('concurrency').value)||1,queue.length));setProgress(0,queue.length,'Creating only missing MP3 files…');async function worker(){while(index<queue.length){var task=queue[index++],res=await processTask(task);if(res&&res.skipped)skipped++;completed++;setProgress(completed,queue.length,'Creating missing MP3 files…');if(index<queue.length)await sleep(250);}}await Promise.all(Array.from({length:workers},worker));state.generating=false;$('scanBtn').disabled=false;syncGenerateButton();setProgress(queue.length,queue.length,'Finished: '+state.uploaded+' uploaded, '+skipped+' already existed, '+state.failed+' failed.');saveHistory({book:state.book?state.book.title:'',attempted:queue.length,uploaded:state.uploaded,skipped:skipped,failed:state.failed,voice:text($('voiceId').value)||'default',at:new Date().toISOString()});renderHistory();renderRows();toast(state.failed?'Finished with '+state.failed+' failure(s). You can retry failed rows.':'Audio update finished.',!!state.failed);}
function saveHistory(item){try{var l=JSON.parse(localStorage.getItem(HISTORY_KEY)||'[]');if(!Array.isArray(l))l=[];l.unshift(item);localStorage.setItem(HISTORY_KEY,JSON.stringify(l.slice(0,12)));}catch(_){}}
function renderHistory(){var l=[];try{l=JSON.parse(localStorage.getItem(HISTORY_KEY)||'[]');}catch(_){}if(!Array.isArray(l)||!l.length){$('history').innerHTML='<div class="sub">No runs yet.</div>';return;}$('history').innerHTML=l.map(function(x){var d='';try{d=new Date(x.at).toLocaleString();}catch(_){}return '<div class="history-item"><b>'+esc(x.book||'Book audio')+'</b><div>'+esc(d)+'</div><div>'+Number(x.uploaded||0)+' uploaded · '+Number(x.skipped||0)+' skipped · '+Number(x.failed||0)+' failed</div></div>';}).join('');}
async function searchCandidates(q){var c=[],norm=normalizeKey(q);if(norm)c.push({key:q,spoken:q,source:'Exact key'});state.tasks.forEach(function(t){if(t.norm.indexOf(norm)>=0||t.spoken.toLowerCase().indexOf(q.toLowerCase())>=0)c.push({key:t.key,spoken:t.spoken,source:state.book?state.book.title:'Current book'});});var pattern='*'+q.replace(/[%*]/g,'')+'*';try{var d=await Promise.all([rest('lexical_entries',{select:'canonical_text,source_key',canonical_text:'ilike.'+pattern,status:'in.(review,published)',limit:'40'},false),rest('sentences',{select:'text,canonical_text,audio_key,source_key',text:'ilike.'+pattern,status:'in.(review,published)',limit:'40'},false),rest('passages',{select:'title,audio_key,source_key',audio_key:'ilike.'+pattern,status:'in.(review,published)',limit:'40'},false)]);d[0].forEach(function(r){if(r.canonical_text)c.push({key:r.canonical_text,spoken:r.canonical_text,source:'Vocabulary DB'});});d[1].forEach(function(r){var s=text(r.canonical_text||r.text);if(s)c.push({key:text(r.audio_key)||s,spoken:s,source:'Sentence DB'});});d[2].forEach(function(r){if(r.audio_key)c.push({key:r.audio_key,spoken:r.title||r.audio_key,source:'Passage DB'});});}catch(e){}var map={};c.forEach(function(x){var n=normalizeKey(x.key);if(n&&!map[n])map[n]=Object.assign({norm:n},x);});return Object.keys(map).map(function(k){return map[k];}).slice(0,120);}
async function searchR2(){var q=text($('r2Search').value);if(!q)return;$('searchStatus').textContent='Searching…';$('searchResults').innerHTML='';$('r2SearchBtn').disabled=true;try{var c=await searchCandidates(q),found=[];if(c.length){var res=await lookupExisting(c.map(function(x){return{key:x.key};}));c.forEach(function(x){var i=res[x.key];if(i&&i.exists)found.push(Object.assign({},x,{url:i.url}));});if(!found.some(function(x){return x.norm===normalizeKey(q);})){var ex=await directExists(q);if(ex.exists)found.unshift({key:q,norm:normalizeKey(q),spoken:q,source:'Exact R2 key',url:ex.url});}}$('searchStatus').textContent=found.length+' R2 file'+(found.length===1?'':'s')+' found from '+c.length+' candidate key'+(c.length===1?'':'s')+'.';$('searchResults').innerHTML=found.length?found.map(function(x){return '<div class="result"><div><strong>'+esc(x.norm)+'.mp3</strong><span class="sub">'+esc(x.source)+' · '+esc(x.spoken)+'</span></div><button class="play" data-r2-play="'+esc(x.url)+'">▶ Play</button></div>';}).join(''):'<div class="empty">No matching R2 audio found.</div>';$('searchResults').querySelectorAll('[data-r2-play]').forEach(function(b){b.addEventListener('click',function(){playUrl(b.dataset.r2Play,b);});});}catch(e){$('searchStatus').textContent='Search failed.';toast(e.message,true);}finally{$('r2SearchBtn').disabled=false;}}
function bind(){document.querySelectorAll('.tab').forEach(function(b){b.addEventListener('click',function(){document.querySelectorAll('.tab').forEach(function(x){x.classList.toggle('active',x===b);});$('generatePanel').hidden=b.dataset.tab!=='generate';$('searchPanel').hidden=b.dataset.tab!=='search';});});bookSelect.addEventListener('change',function(){loadManifest(bookSelect.value);});$('scanBtn').addEventListener('click',scanR2);$('generateBtn').addEventListener('click',generateMissing);[$('rowSearch'),$('typeFilter'),$('stateFilter'),unitFilter].forEach(function(el){el.addEventListener(el.tagName==='INPUT'?'input':'change',renderRows);});$('selectAll').addEventListener('change',function(){var v=new Set(currentTasks().map(function(t){return t.norm;}));state.tasks.forEach(function(t){if(v.has(t.norm)&&(t.state==='missing'||t.state==='failed'))t.selected=$('selectAll').checked;});renderRows();syncGenerateButton();});$('voiceId').value=(function(){try{return localStorage.getItem('ttsVoiceId')||'';}catch(_){return'';}})();$('r2SearchBtn').addEventListener('click',searchR2);$('r2Search').addEventListener('keydown',function(e){if(e.key==='Enter')searchR2();});var c=$('concurrency');if(c)c.value='1';}
(async function(){bind();renderHistory();if(!await verify())return;try{await loadBooks();progressLabel.textContent='Choose a book to build its audio manifest.';}catch(e){bookSelect.innerHTML='<option>Could not load books</option>';toast(e.message,true);}})();
})();