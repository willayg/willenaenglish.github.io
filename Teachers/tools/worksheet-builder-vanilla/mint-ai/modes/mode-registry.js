// mode-registry.js
// Registry for all MINT AI modal modes

import freeMode from './free-mode.js';
import grammarMode from './grammar-mode.js';

const modes = [freeMode, grammarMode];

export default modes;
