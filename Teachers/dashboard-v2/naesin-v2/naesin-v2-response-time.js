(function(){
'use strict';
const ORDER=['grammar','reading','vocab_test','communication','sentences','constructed_response'];
const LABELS={grammar:'문법',reading:'독해',vocab_test:'어휘 문제',communication:'의사소통',sentences:'본문',constructed_response:'서술형'};
const q=(s,r=document)=>r.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const sec=ms=>{const value=Number(ms);if(!Number.isFinite(value)||value<=0)return'—';const seconds=value/1000;return seconds<10?`${seconds.toFixed(1)}초`:`${Math.round(seconds)}초`};
const pct=v=>v==null?'—':`${Math.round(Number(v)||0)}%`;
function styles(){if(q('#na2ResponseTimeStyles'))return;const s=document.createElement('style');s.id='na2ResponseTimeStyles';s.textContent=`
.na2-response-time{margin:18px 0 0;padding:18px;border:1px solid rgba(31,71,78,.11);border-radius:18px;background:#fff}
.na2-response-time-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:14px}
.na2-response-time-head h3{margin:0;color:#203039;font-size:18px;line-height:1.25}.na2-response-time-head p{margin:5px 0 0;color:#718187;font-size:12px;line-height:1.45}
.na2-response-scope{flex:0 0 auto;padding:5px 9px;border-radius:999px;background:#eaf6f6;color:#19777e;font-size:10px;font-weight:800}
.na2-response-skills{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.na2-response-card{padding:15px 16px;border:1px solid rgba(31,71,78,.09);border-radius:15px;background:#f9fbfb}
.na2-response-main{display:flex;align-items:baseline;justify-content:space-between;gap:12px}.na2-response-main b{font-size:14px;color:#253f45}.na2-response-main strong{font-size:25px;line-height:1;color:#167780;white-space:nowrap}
.na2-response-caption{margin-top:5px;color:#6f8085;font-size:11px;font-weight:700}.na2-response-split{display:flex;gap:12px;flex-wrap:wrap;margin-top:12px;padding-top:10px;border-top:1px solid rgba(31,71,78,.08);font-size:11px;color:#65777c}.na2-response-split span b{color:#314e54}
.na2-response-flags{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.na2-response-flag{padding:4px 7px;border-radius:999px;background:#eef3f3;color:#607277;font-size:10px;font-weight:700}.na2-response-flag.fast{background:#eef7f5;color:#34776e}.na2-response-flag.slow{background:#f8f2eb;color:#8a6944}
.na2-response-time-empty{padding:16px;text-align:center;color:#7a8a8f;font-size:12px}.na2-response-time-note{margin-top:12px;color:#87959a;font-size:10px;line-height:1.5}@media(max-width:680px){.na2-response-skills{grid-template-columns:1fr}.na2-response-card{padding:14px}.na2-response-main strong{font-size:23px}}`;document.head.appendChild(s)}
function timingHtml(rows){const map=new Map((Array.isArray(rows)?rows:[]).map(r=>[r.practice_type==null?'__overall__':r.practice_type,r]));const skills=ORDER.map(key=>({key,row:map.get(key)})).filter(x=>x.row&&num(x.row.valid_attempt_count)>0);if(!skills.length)return `<section class="na2-response-time"><div class="na2-response-time-head"><div><h3>문제 풀이 속도</h3><p>각 영역에서 한 문제를 푸는 데 걸리는 보통 시간을 보여줍니다.</p></div><span class="na2-response-scope">현재 시험 범위</span></div><div class="na2-response-time-empty">아직 응답 시간 데이터가 없습니다.</div></section>`;const cards=skills.map(({key,row})=>{const count=num(row.valid_attempt_count),low=count<10;return `<article class="na2-response-card"><div class="na2-response-main"><b>${esc(LABELS[key]||key)}</b><strong>${esc(sec(row.median_response_ms))}</strong></div><div class="na2-response-caption">보통 응답 시간 · ${count}문항${low?' · 표본 적음':''}</div><div class="na2-response-split"><span>정답일 때 <b>${esc(sec(row.median_correct_ms))}</b></span><span>오답일 때 <b>${esc(sec(row.median_wrong_ms))}</b></span></div><div class="na2-response-flags"><span class="na2-response-flag fast">아주 빠른 답 &lt;5초 ${esc(pct(row.fast_under_5s_pct))}</span><span class="na2-response-flag slow">긴 고민 &gt;60초 ${esc(pct(row.slow_over_60s_pct))}</span></div></article>`}).join('');return `<section class="na2-response-time"><div class="na2-response-time-head"><div><h3>문제 풀이 속도</h3><p><b>중앙값</b>을 사용합니다. 즉, 극단적으로 빠르거나 느린 몇 문제보다 학생의 ‘보통 속도’를 보여줍니다.</p></div><span class="na2-response-scope">현재 시험 범위</span></div><div class="na2-response-skills">${cards}</div><div class="na2-response-time-note">10분을 넘긴 기록은 보통 응답 시간 계산에서 제외합니다. 단어 학습 드릴은 예전 기록 방식이 달라 이 표에서는 제외했습니다.</div></section>`}
function render(){styles();const body=q('#na2DetailBody'),active=q('#na2DetailTabs [data-tab="activity"].active');if(!body||!active)return;let mount=q('#na2ResponseTimeMount',body);if(!mount){mount=document.createElement('div');mount.id='na2ResponseTimeMount';const chart=q('.na2-detail-section',body);if(chart)chart.insertAdjacentElement('afterend',mount);else body.appendChild(mount)}const data=window.NaesinV2StudentDetail?.getCurrentData?.()||{};const rows=data.response_times||data.stats?.response_times||[];mount.innerHTML=timingHtml(rows)}
styles();
window.NaesinV2ResponseTime={render,version:'2.0.0-snapshot'};
})();