# Cloudflare Pages / Netlify Redirects
# NOTE: This file is used by BOTH Cloudflare Pages (staging) and Netlify
# Rules are processed top-to-bottom, first match wins

# Preserve Netlify Functions routing (critical - must be first)
/.netlify/functions/*       /.netlify/functions/:splat                         200

# Normalize login URLs (relative redirects - work on any domain)
/login.html                 /students/signin.html                              301
/students/login.html        /students/signin.html                              301

# Normalize teacher URLs (relative redirects)
/Teachers/index.html        /Teachers/signin.html                              301
/Teachers/login.html        /Teachers/signin.html                              301
/teachers/index.html        /Teachers/signin.html                              301
/teachers/login.html        /Teachers/signin.html                              301
/teachers/signin.html       /Teachers/signin.html                              301

# All other routes - serve files directly (no external redirects)
# This allows staging to work without redirecting to production
