name = "log-word-attempt"
main = "src/index.js"
compatibility_date = "2025-12-18"

[vars]
# Set SKIP_POINTS_TOTAL_CALC = "1" to skip expensive points calc
# SKIP_POINTS_TOTAL_CALC = "0"

# Secrets (set via wrangler secret put):
# - SUPABASE_URL
# - SUPABASE_SERVICE_KEY
