name = "willena-analyze-sentence"
main = "src/index.js"
compatibility_date = "2025-12-02"

workers_dev = true

# Route for staging domain
[[routes]]
pattern = "staging.willenaenglish.com/api/analyze-sentence*"
zone_name = "willenaenglish.com"

[ai]
binding = "AI"

[vars]
# Add environment variables here or via wrangler secret put
