name = "progress-summary"
# account_id = "<your-cloudflare-account-id>"
# Ensure the route or zone is set in your wrangler config if publishing to a custom domain
# route = "willenaenglish.com/.netlify/functions/progress_summary"

compatibility_date = "2025-12-01"

[build]
command = "npm run build"

[env.production]
# use secrets via `wrangler secret put` or CI variables
# account_id = "..."
# route = "progress-summary.willena.workers.dev"
