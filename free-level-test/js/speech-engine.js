const SR=window.SpeechRecognition||window.webkitSpeechRecognition;export function supported(){return!!SR}export function listen(lang="en-US"){return new Promise((resolve,reject)=>{if(!SR)return reject(new Error("unsupported"));const r=new SR();r.lang=lang;r.interimResults=false;r.maxAlternatives=3;r.onresult=e=>resolve([...e.results[0]].map(x=>x.transcript));r.onerror=e=>reject(new Error(e.error));r.start()})}export function scoreSpoken(transcripts,rules={}){const clean=s=>(s||"").toLowerCase().replace(/[^a-z0-9' ]/g,"").replace(/\s+/g," ").trim(),variants=transcripts.map(clean);for(const[points,list]of[[3,rules.excellent],[2,rules.acceptable],[1,rules.weak]])if((list||[]).some(x=>variants.includes(clean(x))))return points;if((rules.keywords||[]).some(k=>variants.some(v=>v.includes(clean(k)))))return 1;return 0}