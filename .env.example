# .env.example — copy to .env and fill with real values
# WARNING: do NOT commit your real .env with secrets to the repo.

# ElevenLabs (TTS) API key and default voice
ELEVEN_LABS_API_KEY=
ELEVEN_LABS_DEFAULT_VOICE_ID=
ELEVEN_LABS_MODEL_ID=eleven_monolingual_v1

# R2 / S3-compatible upload credentials used by generator scripts
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
R2_BUCKET_NAME=willena-files

# Optional: override Netlify functions base for local dev
NETLIFY_FUNCTIONS_BASE=http://localhost:9000/.netlify/functions

# Optional extras used by other scripts
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# NOTES:
# - Copy this file to a file named ".env" in the repo root and populate values.
# - Keep that .env out of source control. Use .gitignore if needed.
# - You can run a one-off command in PowerShell instead of creating a .env:
#   $env:ELEVEN_LABS_API_KEY='your_key'; $env:ELEVEN_LABS_DEFAULT_VOICE_ID='voice_id'; node scripts/process_grammar_audio.js --include=... 
