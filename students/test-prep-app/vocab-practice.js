(function(){
'use strict';
const CONTENT='https://gxwfsqxyuufqtitspfqg.supabase.co';
const KEY=['sb_publishable_','G-FYhHfDL4OGdL892gY1Zg_','epdbEeqO'].join('');
const HEAD={apikey:KEY,Authorization:`Bearer ${KEY}`};
const shuffle=a=>{const b=[...a];for(let i=b.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[b[i],b[j]]=[b[j],b[i]]}return b};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s??'').trim().toLowerCase().replace(/[’‘]/g,"'").replace(/\s+/g,' ');
let host=null,restore=[],items=[],mode='cards',queue=[],index=0,score=0,answered=false,lesson='',reviewMode=false,roundWrong=new Set();
async function get(path){const r=await fetch(CONTENT+path,{headers:HEAD,cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()}
async function loadItems(unitId){const occ=await get(`/rest/v1/source_content_occurrences?select=lexical_entry_id,source_text,metadata&unit_id=eq.${encodeURIComponent(unitId)}&occurrence_type=eq.lexical_entry&skill=eq.vocabulary&order=source_text.asc`);const ids=[...new Set((occ||[]).map(x=>x.lexical_entry_id).filter(Boolean))];if(!ids.length)return[];const lex=await get(`/rest/v1/lexical_entries?select=id,canonical_text,translation_ko,entry_type,part_of_speech&id=in.${encodeURIComponent('('+ids.join(',')+')')}`);const byId=new Map((lex||[]).map(x=>[x.id,x])),seen=new Set(),out=[];for(const o of occ||[]){const x=byId.get(o.lexical_entry_id),k=norm(x?.canonical_text||'');if(!x?.canonical_text||!x?.translation_ko||!k||seen.has(k))continue;seen.add(k);out.push(x)}return out}
function hideLegacy(quiz){restore=[];[...quiz.children].forEach(el=>{if(el.id==='assignedBackRow')return;restore.push([el,el.hidden]);el.hidden=true})}
function restoreLegacy(){restore.forEach(([el,h])=>{if(el?.isConnected)el.hidden=h});restore=[];host?.remove();host=null;reviewMode=false}
function startRound(nextItems){queue=shuffle(nextItems);index=0;score=0;answered=false;roundWrong=new Set();render()}
async function modeItems(nextMode){if(nextMode==='cards'||reviewMode)return items;return items}
function allowed(nextMode){if(nextMode==='cards'||reviewMode)return true;return !!window.WillenaVocabProgression?.isUnlocked?.(nextMode)}
async function startMode(nextMode,opts={}){if(!host)return false;if(!allowed(nextMode)&&!opts.force)return false;mode=nextMode;let nextItems;if(Array.isArray(opts.onlyIds)){const wanted=new Set(opts.onlyIds.map(String));nextItems=items.filter(x=>wanted.has(String(x.id)))}else nextItems=await modeItems(nextMode);if(!nextItems.length)return false;startRound(nextItems);return true}
function distractors(correct,field){const seen=new Set(),pool=[];for(const x of shuffle(items)){if(x.id===correct.id)continue;const v=String(x[field]||'').trim(),k=norm(v);if(!v||seen.has(k)||k===norm(correct[field]))continue;seen.add(k);pool.push(v)}return shuffle([correct[field],...pool.slice(0,3)])}
function emitAttempt(q,ok,extra={}){if(!ok)roundWrong.add(String(q.id));const detail={lesson,mode,lexical_entry_id:q.id,canonical_text:q.canonical_text,is_correct:ok,...extra};window.dispatchEvent(new CustomEvent('testprep:vocab-attempt',{detail}));window.WillenaTestPrepAuth?.recordAttempt?.({practice_type:'vocabulary',question_id:q.id,selected_answer:extra.typed_answer??extra.selected_answer??null,correct_answer:mode==='ko-en'||mode==='spelling'?q.canonical_text:q.translation_ko,is_correct:ok,question_type:`vocabulary_${mode}`,targets:['vocabulary',q.part_of_speech||q.entry_type||'lexical_item'],source_label:'lesson_vocabulary',metadata:{lexical_entry_id:q.id,canonical_text:q.canonical_text,mode}})}
function emitStageFinished(){window.dispatchEvent(new CustomEvent('testprep:vocab-stage-finished',{detail:{mode,score:mode==='cards'?queue.length:score,total:queue.length,wrong_ids:[...roundWrong]}}))}
function finish(){const isCards=mode==='cards';if(!isCards)window.WillenaTestPrepAuth?.completeSession?.(score,queue.length,[...roundWrong]);const wrongItems=isCards?[]:queue.filter(x=>roundWrong.has(String(x.id))),hasWrong=wrongItems.length>0;if(reviewMode){host.innerHTML=`<div class="vp-wrap"><div class="vp-result"><div class="vp-score">${score}/${queue.length}</div><h2>Vocabulary 오답 복습 완료</h2><p>${queue.length?Math.round(score/queue.length*100):0}% correct${hasWrong?` · 오답 ${wrongItems.length}개`:''}</p><button class="vp-next" id="vpAgain">${hasWrong?'오답하기':'다음 오답 →'}</button></div></div>`;host.querySelector('#vpAgain').onclick=()=>{if(hasWrong){window.WillenaTestPrepAuth?.beginStudyActivity?.();startRound(wrongItems)}else window.dispatchEvent(new CustomEvent('testprep:review-group-complete'))};return}if(isCards){host.innerHTML=`<div class="vp-wrap"><div class="vp-result"><div class="vp-score">${queue.length}</div><h2>카드 학습 완료</h2><p>단어 ${queue.length}개를 확인했어요.</p><button class="vp-next" id="vpAgain" disabled>다음 단계 확인 중...</button></div></div>`;emitStageFinished();return}host.innerHTML=`<div class="vp-wrap"><div class="vp-result"><div class="vp-score">${score}/${queue.length}</div><h2>${score===queue.length?'완벽해요!':'한 번 더 도전해요'}</h2><p>${queue.length?Math.round(score/queue.length*100):0}% correct${hasWrong?` · 오답 ${wrongItems.length}개`:''}</p><button class="vp-next" id="vpAgain" disabled>다음 단계 확인 중...</button></div></div>`;emitStageFinished()}
function render(){if(!host)return;if(index>=queue.length)return finish();const q=queue[index],isFlash=mode==='cards',isSpelling=mode==='spelling',prompt=mode==='ko-en'||isSpelling?q.translation_ko:q.canonical_text,answer=mode==='ko-en'||isSpelling?q.canonical_text:q.translation_ko,field=mode==='ko-en'?'canonical_text':'translation_ko',choices=(isFlash||isSpelling)?[]:distractors(q,field);answered=false;const progress=Math.max(0,Math.min(100,index/Math.max(1,queue.length)*100));const modeButton=m=>`<button class="vp-mode ${mode===m?'active':''}" data-mode="${m}" ${allowed(m)?'':'disabled'}>${m==='cards'?'카드':m==='ko-en'?'한국어 → 영어':m==='en-ko'?'영어 → 한국어':'Spelling'}</button>`;host.innerHTML=`<div class="vp-wrap"><div class="vp-head"><div><div class="vp-title">${reviewMode?'오답 복습 · ':''}${esc(lesson)} Vocabulary</div><div class="vp-count">${items.length} lexical items</div></div><div class="vp-count">${index+1} / ${queue.length}</div></div>${reviewMode?'':`<div class="vp-modes">${modeButton('cards')}${modeButton('ko-en')}${modeButton('en-ko')}${modeButton('spelling')}</div>`}<div class="vp-progress"><i></i></div><div class="vp-card"><div class="vp-prompt">${esc(prompt)}</div><div class="vp-sub">${esc(q.part_of_speech||q.entry_type||'')}</div>${isFlash?`<div class="vp-flash-answer" id="vpFlashAnswer">${esc(answer)}</div><button class="vp-reveal" id="vpReveal">뜻 보기</button>`:''}${isSpelling?`<input class="vp-spell" id="vpSpell" name="entry_response" autocomplete="new-password" autocorrect="off" autocapitalize="off" spellcheck="false" inputmode="text" placeholder="영어로 입력하세요"><div class="vp-spell-feedback" id="vpSpellFeedback"></div>`:''}${(!isFlash&&!isSpelling)?`<div class="vp-choices">${choices.map(c=>`<button class="vp-choice" data-value="${esc(c)}">${esc(c)}</button>`).join('')}</div>`:''}</div><button class="vp-next" id="vpNext" ${(isFlash||isSpelling)?'':'disabled'}>${isSpelling?'정답 확인':(index===queue.length-1?'끝내기':'다음')}</button></div>`;host.querySelector('.vp-progress i')?.style.setProperty('--progress',`${progress}%`);host.querySelectorAll('.vp-mode').forEach(b=>b.onclick=()=>{const api=window.WillenaVocabProgression;if(api?.startMode)api.startMode(b.dataset.mode);else if(allowed(b.dataset.mode))startMode(b.dataset.mode)});window.WillenaVocabProgression?.applyLocks?.();const next=host.querySelector('#vpNext');if(isFlash){next.disabled=true;host.querySelector('#vpReveal').onclick=()=>{host.querySelector('#vpFlashAnswer').classList.add('show');host.querySelector('#vpReveal').hidden=true;next.disabled=false};next.onclick=()=>{index++;render()}}else if(isSpelling){const input=host.querySelector('#vpSpell'),feedback=host.querySelector('#vpSpellFeedback');input.focus();const check=()=>{if(answered){index++;render();return}const typed=input.value.trim();if(!typed)return;answered=true;const ok=norm(typed)===norm(answer);if(ok)score++;feedback.className='vp-spell-feedback '+(ok?'ok':'bad');feedback.textContent=ok?'정답입니다!':`정답: ${answer}`;input.disabled=true;next.textContent=index===queue.length-1?'끝내기':'다음';emitAttempt(q,ok,{typed_answer:typed})};next.onclick=check;input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();check()}})}else{next.onclick=()=>{if(!answered)return;index++;render()};host.querySelectorAll('.vp-choice').forEach(b=>b.onclick=()=>{if(answered)return;answered=true;const ok=b.dataset.value===answer;if(ok)score++;host.querySelectorAll('.vp-choice').forEach(x=>{if(x.dataset.value===answer)x.classList.add('correct');else if(x===b)x.classList.add('wrong')});next.disabled=false;emitAttempt(q,ok,{selected_answer:b.dataset.value})})}}
async function start(opts){const quiz=opts?.quiz||document.getElementById('assignedQuizPane');if(!quiz||!opts?.unitId)throw new Error('Vocabulary practice could not start.');lesson=opts.lesson||'Lesson';reviewMode=!!opts.reviewMode;hideLegacy(quiz);host=document.createElement('div');host.id='testPrepVocabPractice';quiz.appendChild(host);host.innerHTML='<div class="vp-wrap vp-state-message">단어를 불러오는 중...</div>';items=await loadItems(opts.unitId);if(Array.isArray(opts.onlyIds)&&opts.onlyIds.length){const wanted=new Set(opts.onlyIds.map(String));items=items.filter(x=>wanted.has(String(x.id)))}if(!items.length){host.innerHTML='<div class="vp-wrap vp-state-message">복습할 Vocabulary가 없습니다.</div>';return}window.WillenaTestPrepAuth?.beginStudyActivity?.();mode=reviewMode?'en-ko':'cards';startRound(items)}
window.WillenaVocabPractice={start,restore:restoreLegacy,startMode,get mode(){return mode},get items(){return items.slice()}};
})();