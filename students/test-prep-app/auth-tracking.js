(function(){
'use strict';
const EDGE='https://fiieuiktlsivwfgyivai.supabase.co/functions/v1/test-prep-student',API_KEY='sb_publishable_e-K50PquV9gHdfmefG6tmg_o-vVSl0e',LOGIN='/students/signin.html?next='+encodeURIComponent('/students/test-prep-app/');
const BATCH_STORAGE_KEY='willena_testprep_rev36_attempt_outbox',BATCH_SIZE=5,BATCH_FLUSH_DELAY_MS=12000;
const authPath=a=>`/.netlify/functions/supabase_auth?action=${encodeURIComponent(a)}&_=${Date.now()}`,routedFetch=(url,opts={})=>window.WillenaAPI?.fetch?window.WillenaAPI.fetch(url,{credentials:'include',cache:'no-store',...opts}):fetch(url,{credentials:'include',cache:'no-store',...opts}),localToken=()=>window.WillenaAPI?.getLocalAccessToken?.()||localStorage.getItem('sb_access_token')||'';
const state={user:null,plans:[],plan:null,lesson:null,session:null,sessionSection:null,blocked:true};let activityStartedAt=0,activeAccumulatedMs=0,activeTickAt=0;
let attemptOutbox=[],batchFlushing=false,batchTimer=null,batchSaved=0,batchFailed=0,batchLast='—',batchStatus='Idle';const batchEvents=[];
function emitTracking(type,data){try{window.dispatchEvent(new CustomEvent('testprep:tracking',{detail:{type,at:new Date().toISOString(),...(data||{})}}))}catch(_){}}
function emitState(){try{window.dispatchEvent(new CustomEvent('testprep:student-state-refresh',{detail:{plans:state.plans,tasks:state.plans.flatMap(p=>Array.isArray(p.tasks)?p.tasks:[])}}))}catch(_){}}
function isActive(){return!document.hidden&&document.hasFocus()}
function flushActiveTime(){const now=performance.now();if(activeTickAt)activeAccumulatedMs+=Math.max(0,now-activeTickAt);activeTickAt=isActive()&&activityStartedAt?now:0}
function beginStudyActivity(){activityStartedAt=Date.now();activeAccumulatedMs=0;activeTickAt=isActive()?performance.now():0}
function getActiveTimeMs(){flushActiveTime();return Math.max(0,Math.round(activeAccumulatedMs))}
function resetStudyActivity(){activityStartedAt=0;activeAccumulatedMs=0;activeTickAt=0}
document.addEventListener('visibilitychange',flushActiveTime);window.addEventListener('focus',flushActiveTime);window.addEventListener('blur',flushActiveTime);
function goLogin(){location.replace(LOGIN)}
async function refreshToken(){try{const r=await routedFetch(authPath('refresh')),d=await r.json().catch(()=>({}));if(r.ok&&d?.success&&d.access_token){window.WillenaAPI?.setLocalTokens?.(d.access_token,d.refresh_token||'');return d.access_token}}catch(_){}return''}
async function ensureToken(force=false){if(force)return refreshToken();return localToken()||await refreshToken()}
const wait=ms=>new Promise(r=>setTimeout(r,ms));
async function edge(action,opts={}){const token=await ensureToken();if(!token)throw new Error('AUTH_REQUIRED');const method=String(opts.method||'GET').toUpperCase(),canRetry=method==='GET',maxAttempts=canRetry?3:1;let lastError=null;for(let attempt=1;attempt<=maxAttempts;attempt++){try{const headers={...(opts.headers||{}),Authorization:`Bearer ${token}`,apikey:API_KEY};const r=await fetch(`${EDGE}?action=${encodeURIComponent(action)}`,{...opts,headers,cache:'no-store',credentials:'omit'});if(canRetry&&[502,503,504].includes(r.status)&&attempt<maxAttempts){await wait(attempt===1?250:650);continue}const text=await r.text();let d={};try{d=JSON.parse(text)}catch{throw new Error(`Invalid API response (${r.status})`)}if(r.status===401)throw new Error('AUTH_REQUIRED');if(!r.ok||d.success===false)throw new Error(d.error||`Request failed (${r.status})`);return d}catch(e){lastError=e;if(e?.message==='AUTH_REQUIRED'||!canRetry||attempt>=maxAttempts)throw e;await wait(attempt===1?250:650)}}throw lastError||new Error('Request failed')}
function shellError(err){console.error('[test-prep-app] shell init failed',err);const app=document.querySelector('.app');if(app)app.innerHTML=`<div style="margin:30px auto;max-width:700px;background:#fff;border:1px solid #e5e7eb;border-radius:18px;padding:24px;font-family:system-ui;text-align:center"><b>시험 대비 화면을 불러오지 못했습니다.</b><div style="margin-top:8px;color:#64748b;font-size:12px">${String(err?.message||err||'Unknown shell error')}</div></div>`}
function bootAssignedShell(){if(window.__testPrepShellBooted)return;let tries=0;const tryBoot=()=>{const shell=window.WillenaAssignedTestPrep;if(shell?.init){window.__testPrepShellBooted=true;try{shell.init();setTimeout(()=>{try{if(state.user&&history.state?.tp!=='practice'){window.WillenaTestPrepUX?.renderHome?.();const home=document.getElementById('assignmentHome');if(home&&!home.innerHTML.trim())shell.renderHome?.()}}catch(e){shellError(e)}},80)}catch(e){shellError(e)}return true}return false};if(tryBoot())return;const timer=setInterval(()=>{if(tryBoot())clearInterval(timer);else if(++tries>200){clearInterval(timer);shellError(new Error('Assigned Test Prep shell did not become available'))}},25)}
async function loadMe(){const d=await edge('me');if(!d?.user)throw new Error('AUTH_REQUIRED');state.user=d.user;state.plans=Array.isArray(d.plans)?d.plans:[];state.blocked=false;bootAssignedShell();emitState();return d}
async function initialLoad(){try{if(localToken()){try{await loadMe();return true}catch(e){if(e?.message!=='AUTH_REQUIRED')throw e}}if(!await refreshToken()){goLogin();return false}await loadMe();return true}catch(e){if(e?.message==='AUTH_REQUIRED'){goLogin();return false}console.error('[test-prep-app] auth init failed',e);document.body.innerHTML='<div style="font-family:system-ui;padding:40px;text-align:center">시험 대비 정보를 불러오지 못했습니다.<br><small>'+String(e?.message||e)+'</small><br><button onclick="location.reload()" style="margin-top:16px;padding:10px 16px;border:0;border-radius:10px;background:#19777e;color:#fff;font-weight:700">다시 시도</button></div>';return false}}
const ready=initialLoad();
async function refreshStats(){await ready;try{return await loadMe()}catch(e){console.warn('[test-prep-app] stats refresh failed',e);return null}}
function setActivePlan(plan,lesson){if(state.session)completeSession(0,0,[]);state.plan=plan||null;state.lesson=lesson||null;state.session=null;state.sessionSection=null;resetStudyActivity()}
function inferSelection(){if(state.plan)return;const sel=window.WillenaAssignedTestPrep?.selection;if(sel?.plan){state.plan=sel.plan;state.lesson=sel.lesson||null}}
async function ensureSession(practiceType){await ready;inferSelection();if(!state.user||!state.plan)return null;const p=String(practiceType||'reading').toLowerCase();if(state.session&&state.sessionSection===p)return state.session;if(state.session&&state.sessionSection!==p)await completeSession(0,0,[]);const d=await edge('start_session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({plan_id:state.plan.id,practice_type:p,unit_key:state.lesson||null})});state.session=d.session||null;state.sessionSection=p;if(!activityStartedAt)beginStudyActivity();emitTracking('session_started',{session_id:state.session?.id||null,plan_id:state.plan?.id||null,lesson:state.lesson,practice_type:p});return state.session}
async function awardCorrectPoints(session,request,attempt){if(!request.is_correct||!attempt?.id)return null;try{const payload={event_type:'attempt',session_id:`testprep-${session.id}`,mode:'test_prep',word:String(request.question_id||attempt.id),is_correct:true,answer:Array.isArray(request.selected_answer)?request.selected_answer.join(', '):String(request.selected_answer??''),correct_answer:Array.isArray(request.correct_answer)?request.correct_answer.join(', '):String(request.correct_answer??''),points:2,attempt_index:attempt.attempt_number??null,duration_ms:request.response_time_ms||0,extra:{source:'test_prep',test_prep_attempt_id:attempt.id,plan_id:request.metadata?.plan_id||state.plan?.id||null,lesson:request.metadata?.lesson||state.lesson||null,practice_type:request.metadata?.practice_type||state.sessionSection||null,question_type:request.question_type||null}};const r=await routedFetch('/.netlify/functions/log_word_attempt',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error||`Points request failed (${r.status})`);const header=document.querySelector('[data-tp-points]');const shown=Number(String(header?.textContent||'').replace(/[^0-9.-]/g,''));if(typeof d.points_total==='number'){if(header)header.textContent=String(d.points_total);try{window.dispatchEvent(new CustomEvent('points:update',{detail:{total:d.points_total}}))}catch(_){}}else if(Number.isFinite(shown)){if(header)header.textContent=String(shown+2);try{window.dispatchEvent(new CustomEvent('points:update',{detail:{total:shown+2}}))}catch(_){}}emitTracking('points_awarded',{points:2,attempt_id:attempt.id,question_id:request.question_id,points_total:typeof d.points_total==='number'?d.points_total:null});return d}catch(e){console.warn('[test-prep-app] points award failed',e);emitTracking('points_error',{points:2,attempt_id:attempt?.id||null,error:String(e?.message||e)});return null}}
function loadAttemptOutbox(){try{const raw=localStorage.getItem(BATCH_STORAGE_KEY),parsed=raw?JSON.parse(raw):[];return Array.isArray(parsed)?parsed:[]}catch(_){return[]}}
function persistAttemptOutbox(){try{localStorage.setItem(BATCH_STORAGE_KEY,JSON.stringify(attemptOutbox.slice(-300)))}catch(_){}}
function makeClientAttemptId(){try{return crypto.randomUUID()}catch(_){return'tp-'+Date.now()+'-'+Math.random().toString(36).slice(2,10)}}
function batchEvent(text){batchEvents.unshift(new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'})+'  '+text);if(batchEvents.length>4)batchEvents.length=4;renderBatchRecorder()}
function ensureBatchRecorder(){if(document.getElementById('tpBatchRecorder36'))return;const el=document.createElement('div');el.id='tpBatchRecorder36';el.innerHTML='<div class="tpbr-head"><b>REV36 · BATCH RECORDER</b><span id="tpbrState36">Idle</span></div><div class="tpbr-grid"><div><small>Queued</small><strong id="tpbrQueued36">0</strong></div><div><small>Saved</small><strong id="tpbrSaved36">0</strong></div><div><small>Failed</small><strong id="tpbrFailed36">0</strong></div><div><small>Last batch</small><strong id="tpbrLast36">—</strong></div></div><div id="tpbrEvents36" class="tpbr-events">Waiting for attempts…</div>';const s=document.createElement('style');s.id='tpBatchRecorder36Style';s.textContent='#tpBatchRecorder36{position:fixed;left:10px;bottom:10px;z-index:2147483646;width:min(330px,calc(100vw - 20px));background:rgba(24,39,46,.96);color:#fff;border:1px solid rgba(255,255,255,.18);border-radius:14px;padding:10px 11px;font:700 10px/1.25 Poppins,sans-serif;box-shadow:0 8px 28px rgba(0,0,0,.22);pointer-events:none}.tpbr-head{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:8px}.tpbr-head b{font-size:10px;letter-spacing:.05em}.tpbr-head span{font-size:9px;padding:3px 7px;border-radius:999px;background:#314d58}.tpbr-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.tpbr-grid div{background:rgba(255,255,255,.07);border-radius:9px;padding:6px;min-width:0}.tpbr-grid small{display:block;font-size:8px;opacity:.68;margin-bottom:2px}.tpbr-grid strong{display:block;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tpbr-events{margin-top:7px;padding-top:6px;border-top:1px solid rgba(255,255,255,.12);font:600 8px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace;opacity:.85;white-space:pre-line;min-height:12px}';document.head.appendChild(s);document.body.appendChild(el)}
function renderBatchRecorder(){ensureBatchRecorder();const q=document.getElementById('tpbrQueued36'),sv=document.getElementById('tpbrSaved36'),f=document.getElementById('tpbrFailed36'),l=document.getElementById('tpbrLast36'),st=document.getElementById('tpbrState36'),ev=document.getElementById('tpbrEvents36');if(q)q.textContent=String(attemptOutbox.length);if(sv)sv.textContent=String(batchSaved);if(f)f.textContent=String(batchFailed);if(l)l.textContent=batchLast;if(st){st.textContent=batchStatus;st.style.background=batchFlushing?'#765d1f':batchStatus==='Error'?'#74363d':'#314d58'}if(ev)ev.textContent=batchEvents.length?batchEvents.join('\n'):'Waiting for attempts…'}
function scheduleBatchFlush(){if(batchTimer||!attemptOutbox.length)return;batchTimer=setTimeout(()=>{batchTimer=null;flushAttemptBatch('timer')},BATCH_FLUSH_DELAY_MS)}
function buildQueuedRequest(payload,s){return{client_attempt_id:makeClientAttemptId(),session_id:s.id,question_id:payload.question_id,selected_answer:payload.selected_answer,correct_answer:payload.correct_answer,is_correct:!!payload.is_correct,question_type:payload.question_type||null,targets:Array.isArray(payload.targets)?payload.targets:[],response_time_ms:Number(payload.response_time_ms)||0,metadata:{...(payload.metadata&&typeof payload.metadata==='object'?payload.metadata:{}),source_question_number:payload.source_question_number,question_type:payload.question_type,source_label:payload.source_label,lesson:state.lesson,plan_id:state.plan?.id,practice_type:payload?.practice_type||state.sessionSection}}}
async function sendAttemptBatch(sessionId,attempts){return edge('batch_attempts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({session_id:sessionId,attempts})})}
async function flushAttemptBatch(reason='manual'){if(batchFlushing||!attemptOutbox.length)return;batchFlushing=true;batchStatus='Flushing';if(batchTimer){clearTimeout(batchTimer);batchTimer=null}renderBatchRecorder();const snapshot=attemptOutbox.slice();batchEvent('⇧ FLUSH '+snapshot.length+' ('+reason+')');const groups=new Map();for(const a of snapshot){if(!groups.has(a.session_id))groups.set(a.session_id,[]);groups.get(a.session_id).push(a)}const acknowledged=new Set();try{for(const [sid,attempts] of groups){try{const d=await sendAttemptBatch(sid,attempts),results=Array.isArray(d?.results)?d.results:[];let okCount=0;for(const r of results){if(r?.success&&r.client_attempt_id){acknowledged.add(String(r.client_attempt_id));okCount++;const queued=attempts.find(a=>String(a.client_attempt_id)===String(r.client_attempt_id));if(queued){emitTracking('attempt_saved',{...queued,attempt_id:r?.attempt?.id||null,attempt_number:r?.attempt?.attempt_number??null,is_retry:r?.attempt?.is_retry??null,corrected_previous:r?.attempt?.corrected_previous??null,review:r?.review||null,batch:true,duplicate:!!r?.duplicate});if(queued.is_correct&&!r?.duplicate)await awardCorrectPoints({id:sid},queued,r?.attempt)}}}batchSaved+=okCount;batchLast=attempts.length+' → '+okCount;batchEvent('✓ SAVED '+okCount);if(okCount<attempts.length){batchFailed+=attempts.length-okCount;batchStatus='Error';batchEvent('! RETAIN '+(attempts.length-okCount))}}catch(e){batchFailed+=attempts.length;batchLast=attempts.length+' failed';batchStatus='Error';batchEvent('✕ FAILED '+attempts.length);console.warn('[REV36 batch] canonical batch save failed',e)}}}finally{if(acknowledged.size){attemptOutbox=attemptOutbox.filter(a=>!acknowledged.has(String(a.client_attempt_id)));persistAttemptOutbox()}if(attemptOutbox.length)scheduleBatchFlush();else if(batchStatus!=='Error')batchStatus='Idle';batchFlushing=false;renderBatchRecorder()}}
async function recordAttempt(payload){try{const s=await ensureSession(payload?.practice_type);if(!s)return;const request=buildQueuedRequest(payload,s);attemptOutbox.push(request);persistAttemptOutbox();batchStatus='Queued';batchEvent('＋ '+String(request.metadata?.mastery_target_text||request.question_id).slice(0,28));emitTracking('attempt_queued',{client_attempt_id:request.client_attempt_id,session_id:request.session_id,question_id:request.question_id,practice_type:payload?.practice_type||state.sessionSection});if(attemptOutbox.length>=BATCH_SIZE)await flushAttemptBatch('size');else scheduleBatchFlush();return{queued:true,client_attempt_id:request.client_attempt_id}}catch(e){console.warn('[test-prep-app] attempt queue failed',e);emitTracking('attempt_error',{error:String(e?.message||e)})}}
async function flushSessionAttempts(sessionId,reason='session_complete'){if(!sessionId)return;let guard=0;while(attemptOutbox.some(a=>a.session_id===sessionId)&&guard<4){await flushAttemptBatch(reason);if(batchFlushing)await wait(50);guard++}}
async function completeSession(correctCount,questionCount,wrongIds){const s=state.session;if(!s){resetStudyActivity();return}await flushSessionAttempts(s.id,'complete');const activeTimeMs=getActiveTimeMs();state.session=null;state.sessionSection=null;resetStudyActivity();try{const request={session_id:s.id,correct_count:Number(correctCount)||0,question_count:Number(questionCount)||0,wrong_ids:Array.isArray(wrongIds)?wrongIds:[],active_time_ms:activeTimeMs},d=await edge('complete_session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(request)});emitTracking('session_completed',{...request,session:d?.session||null});return d}catch(e){console.warn('[test-prep-app] session completion failed',e);emitTracking('session_error',{error:String(e?.message||e)})}}
attemptOutbox=loadAttemptOutbox();ensureBatchRecorder();if(attemptOutbox.length){batchStatus='Recovered';batchEvent('↻ recovered '+attemptOutbox.length);setTimeout(()=>flushAttemptBatch('recovery'),1500)}else renderBatchRecorder();document.addEventListener('visibilitychange',()=>{if(document.hidden&&attemptOutbox.length)flushAttemptBatch('hidden')});window.addEventListener('online',()=>{if(attemptOutbox.length)flushAttemptBatch('online')});window.addEventListener('pagehide',persistAttemptOutbox);
window.WillenaTestPrepAuth={ready,state,edge,setActivePlan,recordAttempt,completeSession,ensureSession,beginStudyActivity,getActiveTimeMs,refreshStats,flushAttemptBatch,getAttemptBatchState:()=>({queued:attemptOutbox.length,flushing:batchFlushing,saved:batchSaved,failed:batchFailed,lastBatch:batchLast})};
console.log('[REV36 batch] canonical test-prep-student batching active',{batchSize:BATCH_SIZE,flushDelayMs:BATCH_FLUSH_DELAY_MS});
})();