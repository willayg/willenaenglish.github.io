(function(){
'use strict';
const TRACK='https://fiieuiktlsivwfgyivai.supabase.co';
const TRACK_KEY='sb_publishable_e-K50PquV9gHdfmefG6tmg_o-vVSl0e';
const CONTENT='https://gxwfsqxyuufqtitspfqg.supabase.co';
const CONTENT_KEY=['sb_publishable_','G-FYhHfDL4OGdL892gY1Zg_','epdbEeqO'].join('');
const ORDER=['cards','ko-en','en-ko','spelling'];
const LABEL={cards:'카드', 'ko-en':'한국어 → 영어', 'en-ko':'영어 → 한국어', spelling:'Spelling'};
let key='',loading=false,transitioning=false,entryHost=null,entryBusy=false,reopenedHost=null;
let progress={cards_complete:false,ko_en_complete:false,en_ko_complete:false,spelling_complete:false};
const norm=s=>String(s??'').trim().toLowerCase().replace(/[’‘]/g,"'").replace(/\s+/g,' ');
const token=()=>window.WillenaAPI?.getLocalAccessToken?.()||localStorage.getItem('sb_access_token')||'';
const selection=()=>window.WillenaAssignedTestPrep?.selection;
const uid=()=>window.WillenaTestPrepAuth?.state?.user?.id||'';
const stateKey=s=>s?.plan?.id&&s?.lesson?`${s.plan.id}|${s.lesson}`:'';
function style(){if(document.getElementById('tpVocabFlowSafeStyle'))return;const el=document.createElement('style');el.id='tpVocabFlowSafeStyle';el.textContent=`.tp-vocab-unlock-bg{position:fixed;inset:0;z-index:9999;background:rgba(20,35,42,.42);display:flex;align-items:center;justify-content:center;padding:20px}.tp-vocab-unlock{width:min(420px,100%);background:#fff;border-radius:22px;padding:24px;text-align:center;box-shadow:0 18px 60px rgba(0,0,0,.18)}.tp-vocab-unlock .icon{font-size:38px}.tp-vocab-unlock h3{margin:10px 0 6px;font-size:21px;color:#26383f}.tp-vocab-unlock p{margin:0 0 18px;color:#687b83;font-weight:600;line-height:1.55}.tp-vocab-unlock button{width:100%;border:0;border-radius:14px;padding:13px 16px;background:#19777e;color:#fff;font-weight:800;font-size:15px;cursor:pointer}.tp-vocab-done{padding:28px 18px;text-align:center}.tp-vocab-done-mark{width:58px;height:58px;margin:6px auto 14px;border-radius:50%;display:grid;place-items:center;background:#e9f8f4;color:#187260;font-size:30px;font-weight:900}.tp-vocab-done h2{margin:0 0 8px;color:#26383f;font-size:24px}.tp-vocab-done p{margin:0 auto 18px;max-width:420px;color:#687b83;line-height:1.6;font-weight:600}.tp-vocab-reopen{border:0;background:transparent;color:#52727b;text-decoration:underline;text-underline-offset:3px;font-size:12px;font-weight:700;padding:8px 10px;cursor:pointer}`;document.head.appendChild(el)}
async function getJson(base,path,keyValue){const auth=keyValue===TRACK_KEY?token():keyValue;const r=await fetch(base+path,{headers:{apikey:keyValue,Authorization:`Bearer ${auth}`},cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()}
async function save(patch){const s=selection(),student=uid(),t=token();if(!s?.plan?.id||!s.lesson||!student||!t)return false;progress={...progress,...patch};const body={student_id:student,plan_id:s.plan.id,lesson:s.lesson,...progress,updated_at:new Date().toISOString()};const r=await fetch(`${TRACK}/rest/v1/test_prep_vocab_progress?on_conflict=student_id,plan_id,lesson`,{method:'POST',headers:{apikey:TRACK_KEY,Authorization:`Bearer ${t}`,'Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(body)});if(!r.ok){console.warn('[test-prep] vocab progress save failed',await r.text());return false}applyLocks();window.dispatchEvent(new CustomEvent('testprep:vocab-progress-changed',{detail:{plan_id:s.plan.id,lesson:s.lesson,...progress}}));return true}
async function lexicalWords(){const s=selection();if(!s?.unitId)return[];const occ=await getJson(CONTENT,`/rest/v1/source_content_occurrences?select=lexical_entry_id&unit_id=eq.${encodeURIComponent(s.unitId)}&occurrence_type=eq.lexical_entry&skill=eq.vocabulary`,CONTENT_KEY);const ids=[...new Set((occ||[]).map(x=>String(x.lexical_entry_id||'')).filter(Boolean))];if(!ids.length)return[];const lex=await getJson(CONTENT,`/rest/v1/lexical_entries?select=id,canonical_text,translation_ko&id=in.${encodeURIComponent('('+ids.join(',')+')')}`,CONTENT_KEY);return[...new Set((lex||[]).filter(x=>x?.canonical_text&&x?.translation_ko).map(x=>norm(x.canonical_text)).filter(Boolean))]}
async function cardsAreKnown(){const s=selection(),student=uid();if(!s?.plan?.id||!s.lesson||!student)return false;const words=await lexicalWords();if(!words.length)return false;const q=new URLSearchParams({select:'knew,created_at,metadata',student_id:`eq.${student}`,plan_id:`eq.${s.plan.id}`,lesson:`eq.${s.lesson}`,order:'created_at.asc'});const rows=await getJson(TRACK,`/rest/v1/test_prep_vocab_self_checks?${q}`,TRACK_KEY);const latest=new Map();for(const r of rows||[]){const word=norm(r?.metadata?.canonical_text||'');if(word)latest.set(word,!!r.knew)}return words.every(word=>latest.get(word)===true)}
async function load(force=false){const s=selection(),student=uid(),k=stateKey(s);if(!k||!student||loading||(!force&&k===key))return;key=k;loading=true;progress={cards_complete:false,ko_en_complete:false,en_ko_complete:false,spelling_complete:false};try{const q=new URLSearchParams({select:'cards_complete,ko_en_complete,en_ko_complete,spelling_complete',student_id:`eq.${student}`,plan_id:`eq.${s.plan.id}`,lesson:`eq.${s.lesson}`,limit:'1'});const rows=await getJson(TRACK,`/rest/v1/test_prep_vocab_progress?${q}`,TRACK_KEY);if(rows?.[0])progress={...progress,...rows[0]};if(!progress.cards_complete&&await cardsAreKnown())await save({cards_complete:true})}catch(e){console.warn('[test-prep] vocab progress load failed',e)}finally{loading=false;applyLocks();applyEntry()}}
function unlocked(mode){if(mode==='cards')return true;if(mode==='ko-en')return !!progress.cards_complete;if(mode==='en-ko')return !!progress.ko_en_complete;if(mode==='spelling')return !!progress.en_ko_complete;return false}
function lockTitle(mode){return mode==='ko-en'?'카드를 모두 알고 있어야 열려요':mode==='en-ko'?'한국어 → 영어를 완료하세요':mode==='spelling'?'영어 → 한국어를 완료하세요':''}
function applyLocks(){const root=document.querySelector('#testPrepVocabPractice .vp-wrap');if(!root)return;root.querySelectorAll('.vp-mode').forEach(b=>{const mode=b.dataset.mode||'',on=unlocked(mode);b.disabled=!on;b.classList.toggle('vp-card-locked',!on);b.title=on?'':lockTitle(mode);const lock=b.querySelector('.vp-lock-icon');if(lock)lock.hidden=on})}
function modal(title,text,button,action){document.querySelector('.tp-vocab-unlock-bg')?.remove();const bg=document.createElement('div');bg.className='tp-vocab-unlock-bg';bg.innerHTML=`<div class="tp-vocab-unlock" role="dialog" aria-modal="true"><div class="icon">🔓</div><h3>${title}</h3><p>${text}</p><button type="button">${button}</button></div>`;document.body.appendChild(bg);const b=bg.querySelector('button');b.onclick=()=>{bg.remove();action?.()};b.focus()}
async function startMode(mode,opts={}){if(transitioning||!unlocked(mode))return;transitioning=true;try{await window.WillenaVocabPractice?.startMode?.(mode,opts)}catch(e){console.warn('[test-prep] vocab transition failed',e)}finally{transitioning=false}}
function patchFor(mode){return mode==='ko-en'?{ko_en_complete:true}:mode==='en-ko'?{en_ko_complete:true}:mode==='spelling'?{spelling_complete:true}:{} }
function nextMode(mode){const i=ORDER.indexOf(mode);return i>=0&&i<ORDER.length-1?ORDER[i+1]:null}
function retryButton(mode,wrongIds=[]){const btn=document.querySelector('#testPrepVocabPractice #vpAgain');if(!btn)return;const ids=[...new Set((wrongIds||[]).map(String).filter(Boolean))];btn.disabled=false;if(mode==='cards'){btn.textContent='카드 다시 보기';btn.onclick=()=>startMode('cards');return}btn.textContent=ids.length===1?'틀린 1개 다시 하기':`틀린 ${ids.length}개 다시 하기`;btn.onclick=()=>startMode(mode,{onlyIds:ids})}
function firstUnfinished(){if(!progress.cards_complete)return'cards';if(!progress.ko_en_complete)return'ko-en';if(!progress.en_ko_complete)return'en-ko';if(!progress.spelling_complete)return'spelling';return null}
function showDone(host){if(!host||reopenedHost===host)return;host.innerHTML=`<div class="vp-wrap tp-vocab-done"><div class="tp-vocab-done-mark">✓</div><h2>Vocabulary 완료</h2><p>카드부터 Spelling까지 모두 끝냈어요. 틀렸던 단어가 있다면 오답 복습에는 그대로 남아 있습니다.</p><button type="button" class="tp-vocab-reopen">다시 학습하기</button></div>`;host.querySelector('.tp-vocab-reopen')?.addEventListener('click',()=>{reopenedHost=host;startMode('cards')})}
async function applyEntry(){const s=selection(),host=document.getElementById('testPrepVocabPractice');if(!host||s?.section!=='vocabulary'||s.reviewMode||loading||entryBusy)return;if(entryHost===host)return;entryHost=host;entryBusy=true;try{if(progress.spelling_complete){showDone(host);return}const target=firstUnfinished();if(target&&target!=='cards')await startMode(target)}finally{entryBusy=false}}
async function handleStage(detail){const s=selection();if(s?.section!=='vocabulary'||s.reviewMode)return;const mode=String(detail?.mode||''),got=Number(detail?.score)||0,total=Number(detail?.total)||0,wrongIds=Array.isArray(detail?.wrong_ids)?detail.wrong_ids:[];if(!ORDER.includes(mode)||!total)return;
 if(mode==='cards'){
  const all=progress.cards_complete||await cardsAreKnown();if(!all){retryButton('cards');return}
  if(!progress.cards_complete)await save({cards_complete:true});
  modal('다음 단계가 열렸어요!','모든 카드를 알고 있어요. 이제 한국어 → 영어 단계로 넘어갑니다.','한국어 → 영어 시작',()=>startMode('ko-en'));return;
 }
 if(got!==total){retryButton(mode,wrongIds);return}
 await save(patchFor(mode));
 if(mode==='spelling'){modal('Vocabulary 완료!','틀렸던 단어까지 모두 다시 맞혔어요. Vocabulary 학습은 완료되었습니다. 처음 틀린 기록은 오답 복습에 그대로 남아 있습니다.','레슨으로 돌아가기',()=>document.querySelector('#assignedBackRow .back-assign')?.click());return}
 const next=nextMode(mode);modal('다음 단계가 열렸어요!',`${LABEL[mode]}에서 틀린 단어까지 모두 다시 맞혔어요. ${LABEL[next]} 단계가 영구적으로 열렸습니다.`,`${LABEL[next]} 시작`,()=>startMode(next));
}
function refresh(){style();const s=selection();if(s?.section!=='vocabulary'||s.reviewMode){entryHost=null;reopenedHost=null;return}const host=document.getElementById('testPrepVocabPractice');if(host&&host!==entryHost)load(true);else load();applyLocks();if(!loading)applyEntry()}
function boot(){document.addEventListener('click',e=>{const b=e.target instanceof Element?e.target.closest('#testPrepVocabPractice .vp-mode'):null;if(!b)return;const mode=b.dataset.mode||'';if(!unlocked(mode)){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();applyLocks()}},true);window.addEventListener('testprep:vocab-stage-finished',e=>handleStage(e.detail));new MutationObserver(()=>queueMicrotask(refresh)).observe(document.getElementById('assignedQuizPane')||document.body,{childList:true,subtree:true});setInterval(refresh,1000);refresh()}
window.WillenaVocabProgression={applyLocks,isUnlocked:unlocked,reload:()=>load(true),get state(){return{...progress}}};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();