(function(global){
'use strict';
var CONTENT_URL='https://gxwfsqxyuufqtitspfqg.supabase.co';
var CONTENT_KEY=['sb_publishable_','G-FYhHfDL4OGdL892gY1Zg_','epdbEeqO'].join('');
var HEADERS={apikey:CONTENT_KEY,Authorization:'Bearer '+CONTENT_KEY};
var TARGET=12,FOCUS_TARGET=8,engine=null,pool=[],queue=[],baseIds=new Set(),settled=new Set(),current=null,answered=false,previousScroll=0,sessionTarget=TARGET,sessionMode='smart',adaptiveState={items:[]},currentContext=null;
var card=document.querySelector('.unit-progress-card'),ring=document.querySelector('.progress-ring'),title=document.getElementById('smartProgressTitle'),copy=document.getElementById('smartProgressCopy'),pctEl=document.getElementById('smartDailyPct');
var panel=document.getElementById('smartPracticePanel'),root=document.getElementById('smartActivityRoot'),closeBtn=document.getElementById('smartClose'),countEl=document.getElementById('smartCount');
var headerEyebrow=panel&&panel.querySelector('.practice-toolbar .eyebrow'),headerTitle=panel&&panel.querySelector('.practice-toolbar h2');
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function unique(a){var out=[];a.forEach(function(x){if(x!=null&&out.indexOf(x)<0)out.push(x);});return out;}
function dateKey(){var d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');}
function heroContext(){var b=document.getElementById('bookTitle'),u=document.getElementById('unitTitle');return{bookTitle:String(b&&b.textContent||'').trim(),unitText:String(u&&u.textContent||'').trim()};}
function unitNumber(text){var m=String(text||'').match(/Unit\s*(\d+)/i);return m?Number(m[1]):null;}
function dailyKey(){var c=heroContext();return'willena-smart-daily-v2|'+dateKey()+'|'+c.bookTitle+'|'+c.unitText;}
function readNumber(k){try{var n=Number(localStorage.getItem(k)||0);return Number.isFinite(n)?n:0;}catch(_){return 0;}}
function dailyCount(){return readNumber(dailyKey());}
function addDaily(){try{localStorage.setItem(dailyKey(),String(dailyCount()+1));}catch(_){}paint();}
function ready(){var c=heroContext();return c.bookTitle&&!/^Loading/i.test(c.bookTitle)&&unitNumber(c.unitText);}
function paint(){if(!ring)return;var pct=ready()?Math.round(dailyCount()/TARGET*100):0,fill=Math.min(100,pct);ring.style.background='conic-gradient(var(--pink) 0 '+fill+'%,#dff5f7 '+fill+'% 100%)';if(pctEl)pctEl.textContent=pct+'%';if(title)title.textContent='Smart Study';if(copy)copy.textContent='오늘 목표 '+TARGET+'문항 · 맞춤 복습';}
async function get(path){var r=await fetch(CONTENT_URL+'/rest/v1/'+path,{headers:HEADERS,cache:'no-store'});if(!r.ok)throw new Error('Content DB '+r.status);return r.json();}
async function resolveHeroContext(){var c=heroContext(),n=unitNumber(c.unitText);if(!c.bookTitle||!n)throw new Error('Book/unit not ready');var books=await get('content_books?select=id,title,metadata&title=eq.'+encodeURIComponent(c.bookTitle)+'&status=in.(review,published)&limit=1');if(!books.length)throw new Error('Book not found');var units=await get('content_units?select=id,unit_number,title&book_id=eq.'+encodeURIComponent(books[0].id)+'&unit_number=eq.'+n+'&status=in.(review,published)&limit=1');if(!units.length)throw new Error('Unit not found');return{bookId:books[0].id,bookTitle:books[0].title,unitId:units[0].id,unitNumber:Number(units[0].unit_number),unitTitle:units[0].title};}
function contentKeyFromActivity(a){return String(a.sourceType||'activity')+'|'+String(a.sourceId||'')+'|'+String(a.skill||'');}
function contentKeyFromState(s){return String(s.content_type||'activity')+'|'+String(s.content_id||'')+'|'+String(s.skill||'');}
function stateWorthReview(s){if(!s)return false;var due=!s.next_review_at||Date.parse(s.next_review_at)<=Date.now(),mastery=Number(s.mastery_score);return due||!Number.isFinite(mastery)||mastery<80||Number(s.lapses||0)>0;}
async function loadCurrentPool(ctx){var pack=await global.WillenaPracticePool.loadUnitPool({bookId:ctx.bookId,unitId:ctx.unitId,language:'ko'});return pack.activities;}
async function loadDuePools(ctx,state){var reviewItems=(state.items||[]).filter(stateWorthReview).sort(function(a,b){var ad=Date.parse(a.next_review_at||0)||0,bd=Date.parse(b.next_review_at||0)||0;if(ad!==bd)return ad-bd;return Number(a.mastery_score||0)-Number(b.mastery_score||0);});var unitKeys=[],unitMap={};reviewItems.forEach(function(s){var k=s.book_id+'|'+s.unit_id;if(k===ctx.bookId+'|'+ctx.unitId)return;if(!unitMap[k]&&unitKeys.length<5){unitMap[k]=[];unitKeys.push(k);}if(unitMap[k])unitMap[k].push(s);});var result=[];for(var i=0;i<unitKeys.length;i++){var parts=unitKeys[i].split('|'),bookId=parts[0],unitId=parts[1];try{var pack=await global.WillenaPracticePool.loadUnitPool({bookId:bookId,unitId:unitId,language:'ko'}),wanted={};unitMap[unitKeys[i]].forEach(function(s){wanted[contentKeyFromState(s)]=true;});pack.activities.forEach(function(a){if(wanted[contentKeyFromActivity(a)]){a.metadata=Object.assign({},a.metadata,{adaptive_review:true});result.push(a);}});}catch(_){}}
 return result;}
async function loadSameLevelPool(ctx){try{var books=await global.WillenaPracticePool.findSameLevelBooks(ctx.bookTitle);if(!books.length)return[];var book=books[Math.floor(Math.random()*books.length)],units=await global.WillenaPracticePool.loadBookUnits(book.id);if(!units.length)return[];var unit=units.find(function(u){return Number(u.unit_number)===ctx.unitNumber;})||units[Math.floor(Math.random()*units.length)];var pack=await global.WillenaPracticePool.loadUnitPool({bookId:book.id,unitId:unit.id,language:'ko'});return pack.activities.map(function(a){a.metadata=Object.assign({},a.metadata,{same_level_extension:true});return a;});}catch(_){return[];}}
async function buildAdaptivePool(options){options=options||{};if(!global.WillenaPracticePool||!global.WillenaAdaptiveStudy)throw new Error('Adaptive study modules unavailable');adaptiveState=global.WillenaStudyProgress?await global.WillenaStudyProgress.getAdaptiveState():{items:[]};
 if(options.focusUnitId&&options.focusBookId){var focusPack=await global.WillenaPracticePool.loadUnitPool({bookId:options.focusBookId,unitId:options.focusUnitId,language:'ko'});pool=focusPack.activities;return;}
 currentContext=await resolveHeroContext();var parts=await Promise.all([loadCurrentPool(currentContext),loadDuePools(currentContext,adaptiveState),loadSameLevelPool(currentContext)]);pool=parts[0].concat(parts[1]).concat(parts[2]);}
function showSmartScreen(){previousScroll=window.scrollY||0;panel.hidden=false;panel.style.position='fixed';panel.style.inset='0';panel.style.zIndex='10000';panel.style.margin='0';panel.style.borderRadius='0';panel.style.width='100vw';panel.style.height='100dvh';panel.style.maxHeight='100dvh';panel.style.overflowY='auto';panel.style.overflowX='hidden';panel.style.boxSizing='border-box';panel.style.background='linear-gradient(180deg,#dff7f8 0,#effafb 34%,#f7fafb 100%)';panel.style.padding='clamp(12px,3.5vw,34px)';panel.scrollTop=0;}
function closeSmartScreen(){panel.hidden=true;paint();window.scrollTo({top:previousScroll,left:0,behavior:'auto'});}
function setHeader(mode,detail){if(!headerEyebrow||!headerTitle)return;if(mode==='focus'){headerEyebrow.textContent=(detail.skillLabel||detail.skill||'SKILL').toUpperCase();headerTitle.textContent='약한 부분 집중 연습';}else{headerEyebrow.textContent='SMART STUDY';headerTitle.textContent='오늘의 맞춤 연습';}}
function makeSession(options){options=options||{};var selectorOptions={target:options.target||TARGET,currentBookId:currentContext&&currentContext.bookId,currentUnitId:currentContext&&currentContext.unitId,focusSkill:options.focusSkill||null,focusUnitId:options.focusUnitId||null};var chosen=global.WillenaAdaptiveStudy.chooseSession(pool,adaptiveState,selectorOptions);sessionTarget=chosen.length;if(!sessionTarget)throw new Error('No useful activities are available yet');queue=chosen.slice();baseIds=new Set(chosen.map(function(a){return a.id;}));settled=new Set();renderNext();}
function renderNext(){if(!queue.length){finish();return;}current=queue.shift();answered=false;countEl.textContent=Math.min(sessionTarget,settled.size+1)+' / '+sessionTarget;engine=new WillenaActivityEngine(root,{onAnswer:function(){}});engine.setActivity(current);panel.scrollTop=0;}
function turnCheckIntoNext(){var check=root.querySelector('.activity-check');if(!check)return;var replacement=check.cloneNode(true);replacement.disabled=false;replacement.textContent=queue.length?'다음':'완료';check.replaceWith(replacement);replacement.addEventListener('click',renderNext,{once:true});}
function onAnswer(e){if(panel.hidden||!current||answered)return;var d=e.detail||{},a=d.activity||{},r=d.result||{};if(a.id!==current.id)return;answered=true;if(r.correct){if(baseIds.has(current.id)&&!settled.has(current.id)){settled.add(current.id);if(sessionMode==='smart')addDaily();}}else{global.WillenaAdaptiveStudy.insertRetry(queue,current);}turnCheckIntoNext();}
function finish(){countEl.textContent='완료';var msg=sessionMode==='focus'?'약한 부분을 집중해서 연습했어요.':'오늘 필요한 복습과 새 학습을 모두 끝냈어요.';root.innerHTML='<div class="activity-card smart-finish"><div class="smart-confetti" aria-hidden="true">🎉 ✨ 🎊 ⭐ 🎉</div><span class="eyebrow">'+(sessionMode==='focus'?'FOCUSED PRACTICE':'SMART STUDY')+'</span><h2>잘했어요!</h2><p>'+sessionTarget+'개 활동을 끝냈어요.</p><p class="smart-finish-note">'+msg+'</p><button type="button" class="activity-check smart-home-button">학습 화면으로 돌아가기</button></div>';var back=root.querySelector('.smart-home-button');if(back)back.addEventListener('click',closeSmartScreen,{once:true});paint();}
async function startSmart(e){if(e){e.preventDefault();e.stopPropagation();}if(!ready())return;sessionMode='smart';setHeader('smart',{});showSmartScreen();root.innerHTML='<div class="study-loading">오늘 가장 도움이 되는 문제를 고르는 중…</div>';countEl.textContent='— / '+TARGET;try{currentContext=await resolveHeroContext();await buildAdaptivePool({});makeSession({target:TARGET});}catch(err){root.innerHTML='<div class="study-loading">Smart Study를 불러오지 못했습니다. '+esc(err.message||err)+'</div>';}}
async function startFocus(detail){detail=detail||{};if(!detail.bookId||!detail.unitId||!detail.skill)return;sessionMode='focus';setHeader('focus',detail);showSmartScreen();root.innerHTML='<div class="study-loading">약한 '+esc(detail.skillLabel||detail.skill)+' 문제를 찾는 중…</div>';countEl.textContent='—';try{currentContext=await resolveHeroContext().catch(function(){return null;});await buildAdaptivePool({focusBookId:detail.bookId,focusUnitId:detail.unitId});makeSession({target:FOCUS_TARGET,focusSkill:detail.skill,focusUnitId:detail.unitId});}catch(err){root.innerHTML='<div class="study-loading">집중 연습을 준비하지 못했습니다. '+esc(err.message||err)+'</div>';}}
if(!card||!panel)return;card.setAttribute('role','button');card.setAttribute('tabindex','0');card.setAttribute('aria-label','Start Smart Study');card.style.cursor='pointer';card.addEventListener('click',startSmart,true);card.addEventListener('keydown',function(e){if(e.key==='Enter'||e.key===' '){startSmart(e);}},true);closeBtn.addEventListener('click',closeSmartScreen);global.addEventListener('willena:activity-answer',onAnswer);global.addEventListener('willena:smart-study-focus',function(e){startFocus(e.detail||{});});paint();var tries=0,t=setInterval(function(){tries++;paint();if((ready()&&tries>5)||tries>30)clearInterval(t);},350);window.addEventListener('pageshow',function(){setTimeout(paint,100);});
})(window);
