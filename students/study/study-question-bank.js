(function(global){
'use strict';
var URL='https://gxwfsqxyuufqtitspfqg.supabase.co';
var KEY=['sb_publishable_','G-FYhHfDL4OGdL892gY1Zg_','epdbEeqO'].join('');
var HEADERS={apikey:KEY,Authorization:'Bearer '+KEY};
var ALLOWED={grammar:'grammar',grammar_error:'grammar',grammar_application:'grammar',question_response:'conversation',vocabulary:'vocabulary',listening:'listening',sentence_unscramble:'sentence_building'};
var SELECT='id,level_id,difficulty_rating,item_type,prompt_text,context_text,correct_answer,metadata,choices,anchor_pattern_id';
function arr(v){return Array.isArray(v)?v:[];}
function text(v){return String(v==null?'':v).trim();}
async function get(path){var r=await fetch(URL+'/rest/v1/'+path,{headers:HEADERS,cache:'no-store'});if(!r.ok)throw new Error('Question bank '+r.status);return r.json();}
async function getAll(path){var out=[],offset=0,pageSize=1000;while(true){var sep=path.indexOf('?')>=0?'&':'?';var rows=arr(await get(path+sep+'limit='+pageSize+'&offset='+offset));out=out.concat(rows);if(rows.length<pageSize)break;offset+=pageSize;if(offset>100000)throw new Error('Question bank pagination safety limit exceeded');}return out;}
function optionTexts(row){return arr(row.choices).map(function(x){return typeof x==='string'?x:text(x&&x.text||x&&x.option_text);}).filter(Boolean);}
function answer(row){var a=row.correct_answer;if(typeof a==='string'||typeof a==='number')return String(a);if(a&&typeof a==='object')return text(a.text||a.answer||a.value);return'';}
function splitDialogueOrder(value){return text(value).split(/\s*→\s*/).map(text).filter(Boolean);}
function quotedSpeech(value){var s=text(value),m=s.match(/[“\"]([^”\"]+)[”\"]/);return m?text(m[1]):'';}
function conversationPresentation(row,meta,choices,correct){var form=text(meta.conversation_form||meta.question_form),prompt=text(row.prompt_text),context=text(row.context_text),response={type:'multiple_choice',choices:choices},answerValue=correct;
 if(form==='dialogue_order'){var tokens=splitDialogueOrder(correct);if(tokens.length>1){response={type:'token_order',tokens:tokens};answerValue=tokens;}return{prompt:'대화를 자연스러운 순서로 배열하세요.',context:context,response:response,answer:answerValue};}
 if(form==='missing_line')return{prompt:'빈칸에 들어갈 가장 자연스러운 말을 고르세요.',context:context,response:response,answer:answerValue};
 if(form==='translation_en_ko')return{prompt:prompt,context:'가장 알맞은 한국어 뜻을 고르세요.',response:response,answer:answerValue};
 if(form==='translation_ko_en')return{prompt:prompt,context:'가장 알맞은 영어 표현을 고르세요.',response:response,answer:answerValue};
 if(form==='matching_question')return{prompt:prompt,context:'이 대답에 가장 잘 맞는 질문을 고르세요.',response:response,answer:answerValue};
 if(form==='meaning')return{prompt:'대화를 읽고 가장 알맞은 답을 고르세요.',context:context,response:response,answer:answerValue};
 if(form==='situation'){var said=quotedSpeech(prompt),cleanPrompt=prompt;if(said)cleanPrompt=prompt.replace(/[“\"]([^”\"]+)[”\"]/,'').replace(/\s+/g,' ').replace(/\s+([?.!,])/g,'$1').trim();return{prompt:'가장 자연스러운 대답을 고르세요.',context:(said?'상대방이 말했어요:\n“'+said+'”\n\n':'')+(context||cleanPrompt),response:response,answer:answerValue};}
 return{prompt:prompt||'가장 알맞은 응답을 고르세요.',context:context||'가장 알맞은 응답을 고르세요.',response:response,answer:answerValue};
}
function sentencePresentation(row,meta,correct){var toks=arr(meta.tokens).map(text).filter(Boolean);if(!toks.length)toks=text(correct).split(/\s+/).filter(Boolean);return{prompt:text(row.prompt_text)||'단어를 눌러 올바른 순서로 문장을 만드세요.',context:text(row.context_text)||'',response:{type:'token_order',tokens:toks},answer:correct};}
function mapRow(row,context){var meta=row.metadata||{},skill=ALLOWED[text(row.item_type)];if(text(row.item_type)==='vocabulary'&&(text(meta.skill)==='spelling'||text(meta.question_family)==='spelling'))skill='spelling';if(!skill)return null;var choices=optionTexts(row),correct=answer(row),presentation=null;if(skill==='sentence_building')presentation=sentencePresentation(row,meta,correct);else{if(correct&&choices.indexOf(correct)<0)choices=choices.concat([correct]);if(!correct||choices.length<2)return null;if(skill==='conversation')presentation=conversationPresentation(row,meta,choices,correct);}if(!correct)return null;var patternId=row.anchor_pattern_id||meta.pattern_id||null;var sourceBook=text(meta.source_book_title||meta.book_title),sourceUnit=Number(meta.source_unit_number||meta.unit_number)||null;var bookSpecific=!!(sourceBook&&sourceUnit);var sourceLabel=bookSpecific?'Book '+skill.replace('_',' ')+' bank':'Approved level bank';var feedback=null;if(skill==='grammar'&&meta.feedback_enabled&&text(meta.explanation_ko))feedback={showOnWrong:true,explanation_ko:text(meta.explanation_ko)};
 return{id:'assessment-'+row.id,sourceType:'assessment_item',sourceId:row.id,skill:skill,usage:['practice','level_test'],stimulus:{type:'text',prompt:presentation?presentation.prompt:text(row.prompt_text),context:presentation?presentation.context:(text(row.context_text)||(skill==='conversation'?'가장 알맞은 응답을 고르세요.':'알맞은 답을 고르세요.'))},response:presentation?presentation.response:{type:'multiple_choice',choices:choices},answer:presentation?presentation.answer:correct,feedback:feedback,level:Number(row.level_id)||null,difficulty:Number(row.difficulty_rating)||null,metadata:{book_id:context.bookId,unit_id:context.unitId,pool_source:'assessment_bank',source_label:sourceLabel,assessment_item_type:row.item_type,bank_level_id:Number(row.level_id)||null,authored:true,practice_bank:true,pattern_code:meta.pattern_code||null,pattern_id:patternId,mastery_content_type:(skill==='grammar'&&patternId)?'pattern':null,mastery_content_id:(skill==='grammar'&&patternId)?patternId:null,book_specific:bookSpecific,source_book_title:sourceBook||null,source_unit_number:sourceUnit,question_form:meta.question_form||meta.conversation_form||null,conversation_form:meta.conversation_form||null}};
}
async function resolveBankLevel(publicLevel,context){if(!publicLevel)return null;var bookId=context&&context.bookId;if(bookId){try{var books=await get('content_books?select=internal_level_id,public_level&id=eq.'+encodeURIComponent(bookId)+'&status=in.(review,published)&limit=1');if(books.length){var internal=Number(books[0].internal_level_id)||null,pub=Number(books[0].public_level)||Number(publicLevel);if(internal&&internal<=2)return internal;if(pub)return pub+2;}}catch(_){}}return Number(publicLevel)+2;}
function basePath(bankLevel){return'assessment_items?select='+encodeURIComponent(SELECT)+'&level_id=eq.'+encodeURIComponent(bankLevel)+'&status=eq.published&is_flagged=eq.false';}
async function loadUnit(publicLevel,context){context=context||{};if(!context.bookTitle||context.unitNumber==null)return loadLevel(publicLevel,context);var bankLevel=await resolveBankLevel(publicLevel,context);if(!bankLevel)return[];var path=basePath(bankLevel)+'&metadata->>source_book_title=eq.'+encodeURIComponent(context.bookTitle)+'&metadata->>source_unit_number=eq.'+encodeURIComponent(String(context.unitNumber))+'&order=difficulty_rating.asc,source_key.asc';var rows=arr(await get(path));return rows.map(function(r){return mapRow(r,context);}).filter(Boolean);}
async function loadLevel(publicLevel,context){context=context||{};var bankLevel=await resolveBankLevel(publicLevel,context);if(!bankLevel)return[];var rows=await getAll(basePath(bankLevel)+'&order=difficulty_rating.asc,source_key.asc');return arr(rows).map(function(r){return mapRow(r,context);}).filter(Boolean);}
global.WillenaStudyQuestionBank={version:'authored-bank-v9-unit-first',loadUnit:loadUnit,loadLevel:loadLevel};
})(window);
