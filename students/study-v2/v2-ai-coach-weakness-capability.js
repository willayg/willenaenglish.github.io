(function(global){
'use strict';
var coach=global.WillenaAICoach;
if(!coach||typeof coach.registerCapability!=='function')return;

function text(v){return String(v==null?'':v).trim();}
function arr(v){return Array.isArray(v)?v:[];}
function ko(){var b=document.getElementById('languageBtn');return !b||text(b.textContent)==='English';}
function skillNameFor(lang,s){var K={vocabulary:'어휘',spelling:'철자',grammar:'문법',sentence_building:'문장 만들기',conversation:'회화',listening:'듣기',reading:'읽기'};var E={vocabulary:'vocabulary',spelling:'spelling',grammar:'grammar',sentence_building:'sentence building',conversation:'conversation',listening:'listening',reading:'reading'};return(lang==='ko'?K:E)[s]||s;}
function skillName(s){return skillNameFor(ko()?'ko':'en',s);}
function history(){var h=global.WillenaCoachHistory;return h&&typeof h.getSnapshot==='function'?h.getSnapshot():null;}
function masteryRows(){var h=history(),current=h&&h.currentUnit&&h.currentUnit.skillMastery;if(Array.isArray(current)&&current.length)return current.map(function(x){return{skill:text(x.skill),pct:Math.max(0,Math.min(100,Number(x.mastery)||0))};});var overall=h&&h.skillMastery;return(Array.isArray(overall)?overall:[]).map(function(x){return{skill:text(x.skill),pct:Math.max(0,Math.min(100,Number(x.mastery)||0))};});}
function recentMisses(){var h=history(),counts={};arr(h&&h.recentAttempts).slice(0,60).forEach(function(a){var skill=text(a&&a.skill);if(!skill||a.correct)return;counts[skill]=(counts[skill]||0)+1;});return counts;}
function assignedLocation(bookId,unitId,ctx){bookId=text(bookId);unitId=text(unitId);var books=ctx&&Array.isArray(ctx.books)?ctx.books:[],book=books.find(function(b){return String(b&&b.book_id)===bookId;})||null;if(!book)return null;var unit=arr(book.units).find(function(u){return String(u&&u.id)===unitId;})||null;if(!unit)return null;return{bookId:bookId,unitId:unitId,bookTitle:text(book.book_title||book.title)||'Book',unitNumber:Number(unit.unit_number)||0,unitTitle:text(unit.title)};}
function locationForSkill(skill,ctx){var h=history(),rows=arr(h&&h.skillLocations).filter(function(x){return text(x&&x.skill)===skill&&assignedLocation(x&&x.bookId,x&&x.unitId,ctx);});rows.sort(function(a,b){return(Number(a.mastery)||0)-(Number(b.mastery)||0)||(Number(b.lapses)||0)-(Number(a.lapses)||0)||(Number(b.attempts)||0)-(Number(a.attempts)||0);});if(rows.length){var a=assignedLocation(rows[0].bookId,rows[0].unitId,ctx);return Object.assign(a,{mastery:Number(rows[0].mastery)||0,lapses:Number(rows[0].lapses)||0,attempts:Number(rows[0].attempts)||0,reason:'weakness_history'});}var mistake=arr(h&&h.recentMistakes).find(function(x){return text(x&&x.skill)===skill&&assignedLocation(x&&x.bookId,x&&x.unitId,ctx);});if(mistake){var m=assignedLocation(mistake.bookId,mistake.unitId,ctx);return Object.assign(m,{reason:'recent_mistake'});}return null;}
function grammarCode(){var h=history(),weak=arr(h&&h.grammar&&h.grammar.weak);for(var i=0;i<weak.length;i++){var code=text(weak[i]&&weak[i].key);if(code&&code!=='grammar')return code;}return'';}
function currentHasSkill(skill,ctx){var book=ctx&&ctx.book;if(!book)return false;var available=arr(book.availableSkills).map(text);if(available.indexOf(skill)>=0)return true;var unit=book.currentUnit||ctx.unit,meta=unit&&unit.metadata||{},modes=arr(meta.study_practice&&meta.study_practice.modes).map(text);return modes.indexOf(skill)>=0;}
function locationLabel(loc){return loc?loc.bookTitle+' · Unit '+loc.unitNumber:'';}
function weakSkills(ctx){var base=masteryRows(),misses=recentMisses(),by={};base.forEach(function(x){if(x.skill)by[x.skill]={skill:x.skill,pct:x.pct,misses:Number(misses[x.skill])||0};});Object.keys(misses).forEach(function(skill){if(!by[skill])by[skill]={skill:skill,pct:100,misses:Number(misses[skill])||0};});var ranked=Object.keys(by).map(function(k){var x=by[k];x.effective=Math.min(x.pct,x.misses>=2?Math.max(25,90-x.misses*10):x.pct);x.location=locationForSkill(x.skill,ctx);x.grammarCode=x.skill==='grammar'?grammarCode():'';x.canStudy=!!(x.location||x.grammarCode||currentHasSkill(x.skill,ctx));return x;}).filter(function(x){return x.skill&&((x.pct>0&&x.pct<80)||x.misses>=2);}).sort(function(a,b){return(a.effective-b.effective)||(b.misses-a.misses)||a.skill.localeCompare(b.skill);});if(ranked.length)return ranked.slice(0,3);var fallback=base.filter(function(x){return x.skill&&x.pct>0;}).sort(function(a,b){return(a.pct-b.pct)||a.skill.localeCompare(b.skill);});if(!fallback.length||fallback[0].pct>=90)return[];var f={skill:fallback[0].skill,pct:fallback[0].pct,misses:0,effective:fallback[0].pct};f.location=locationForSkill(f.skill,ctx);f.grammarCode=f.skill==='grammar'?grammarCode():'';f.canStudy=!!(f.location||f.grammarCode||currentHasSkill(f.skill,ctx));return[f];}
function names(xs,lang){return xs.map(function(x){return skillNameFor(lang,x.skill);});}
function joinNames(xs,lang){var ns=names(xs,lang);if(ns.length<=1)return ns[0]||'';if(lang==='ko')return ns.join(' · ');if(ns.length===2)return ns[0]+' and '+ns[1];return ns[0]+', '+ns[1]+' and '+ns[2];}

coach.registerCapability({
  id:'weakness',
  available:function(ctx){return weakSkills(ctx).length>0;},
  score:function(ctx){var xs=weakSkills(ctx),x=xs[0];return x?Math.max(70,120-(Number(x.effective)||x.pct))+Math.min(2,xs.length)*0.01:0;},
  label:function(ctx){var xs=weakSkills(ctx);if(!xs.length)return{ko:'약한 부분 연습',en:'Practice weak areas'};if(xs.length===1)return{ko:skillNameFor('ko',xs[0].skill)+'을 더 연습할래요',en:'More '+skillNameFor('en',xs[0].skill)+' practice'};return{ko:'약한 영역 '+xs.length+'개 연습하기',en:'Practice '+xs.length+' weak areas'};},
  response:function(ctx){var xs=weakSkills(ctx);if(!xs.length)return'';if(xs.length===1){var x=xs[0],where=locationLabel(x.location);if(where)return ko()?where+'에서 '+skillName(x.skill)+'이 가장 먼저 챙기기 좋은 영역이에요. 공부 내용을 먼저 확인하거나 바로 연습할 수 있어요.':skillNameFor('en',x.skill)+' needs the most attention in '+where+'. You can study that unit first or go straight to practice.';if(x.grammarCode)return ko()?'문법 학습 기록에서 '+skillName(x.skill)+' 관련 개념을 다시 보면 도움이 될 것 같아요.':'Your grammar history points to a specific concept worth reviewing.';return ko()?skillName(x.skill)+'이 지금 가장 먼저 챙기기 좋은 영역이에요.':'Your '+skillName(x.skill)+' looks like the best place to focus right now.';}var located=xs.filter(function(x){return x.location;});if(located.length){var details=located.map(function(x){return locationLabel(x.location)+' · '+skillNameFor(ko()?'ko':'en',x.skill);}).join(ko()?' / ':'; ');return ko()?joinNames(xs,'ko')+'에서 도움이 필요한 부분을 찾았어요. '+details+' 순서로 확인해 볼 수 있어요.':'I found useful work in '+joinNames(xs,'en')+'. The evidence points to '+details+'.';}return ko()?joinNames(xs,'ko')+'에서 도움이 될 만한 부분을 찾았어요.':'I found useful work in '+joinNames(xs,'en')+'.';},
  actions:function(ctx){var out=[];weakSkills(ctx).forEach(function(x){var loc=x.location,where=locationLabel(loc);if(x.canStudy){var args={skill:x.skill};if(loc){args.bookId=loc.bookId;args.unitId=loc.unitId;}if(x.grammarCode)args.codes=[x.grammarCode];out.push({label:{ko:(where?where+' · ':'')+skillNameFor('ko',x.skill)+' 공부하기',en:'Study '+skillNameFor('en',x.skill)+(where?' · '+where:'')},provider:'studyNavigation',args:args});}out.push({label:{ko:skillNameFor('ko',x.skill)+' 집중 연습',en:'Practice '+skillNameFor('en',x.skill)},provider:'unit',args:{skill:x.skill,count:10}});});return out;}
});
})(window);
