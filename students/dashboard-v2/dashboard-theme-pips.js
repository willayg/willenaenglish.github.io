(()=>{
'use strict';
const STORAGE_KEY='willena-dashboard-theme';
const THEMES=['white','dark','cyberpunk'];
function applyTheme(name,{save=true}={}){const chosen=THEMES.includes(name)?name:'white';const root=document.documentElement;root.dataset.dashboardTheme=chosen;document.querySelectorAll('.dashboard-theme-pip[data-theme]').forEach(button=>button.setAttribute('aria-pressed',String(button.dataset.theme===chosen)));const darkToggle=document.getElementById('darkToggle');if(darkToggle)darkToggle.checked=chosen==='dark'||chosen==='cyberpunk';if(save){try{localStorage.setItem(STORAGE_KEY,chosen)}catch{}}}
function addThemePips(){const header=document.querySelector('.header');if(!header||document.getElementById('dashboardThemeDock'))return;const dock=document.createElement('div');dock.id='dashboardThemeDock';dock.className='dashboard-theme-dock';dock.setAttribute('role','group');dock.setAttribute('aria-label','Dashboard theme');dock.innerHTML=[['white','White'],['dark','Dark'],['cyberpunk','Cyber']].map(([theme,label])=>`<button type="button" class="dashboard-theme-pip" data-theme="${theme}" aria-label="${label} theme" title="${label}" aria-pressed="false"></button>`).join('');dock.addEventListener('click',event=>{const button=event.target.closest('.dashboard-theme-pip[data-theme]');if(button)applyTheme(button.dataset.theme)});header.appendChild(dock)}
function boot(){addThemePips();let saved='white';try{saved=localStorage.getItem(STORAGE_KEY)||'white'}catch{}if(!THEMES.includes(saved))saved='white';applyTheme(saved,{save:false});const darkToggle=document.getElementById('darkToggle');darkToggle?.addEventListener('change',()=>applyTheme(darkToggle.checked?'dark':'white'))}
window.WillenaDashboardTheme={apply:applyTheme};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();