name: Cache bust Study V2 tablet rail CSS once

on:
  push:
    branches:
      - staging
    paths:
      - .github/workflows/cache-bust-study-v2-interactions-once.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: staging
      - name: Bust tablet rail CSS cache
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('students/study-v2/index.html')
          s = p.read_text()
          old = './v2-daily-rail-impact.css?v=20260816-railimpact4'
          new = './v2-daily-rail-impact.css?v=20260816-tablethero1'
          if old in s:
              s = s.replace(old, new)
          p.write_text(s)
          PY
      - name: Commit cache bust
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name 'willayg'
          git config user.email 'william.gaule@gmail.com'
          git add students/study-v2/index.html
          git commit -m 'Bust Study V2 tablet rail CSS cache'
          git push origin staging
      - name: Trigger staging deploy hook
        env:
          CF_PAGES_STAGING_HOOK: ${{ secrets.CF_PAGES_STAGING_HOOK }}
        run: |
          if [ -n "$CF_PAGES_STAGING_HOOK" ]; then curl -fsS -X POST "$CF_PAGES_STAGING_HOOK"; fi
